# 2026-05-14

- 评估两条路线后, 决定**优先严格对齐 TransIP-S paper baseline**
  - 比"自己设计"更可靠的对照点 — TransIP-S 是 plain transformer + augmentation + L_eqc + 单层 RBF distance bias 的成熟配方
  - 拿 paper 已发布的 E/F 数字 (3.99 / 31.90 meV) 当锚, 先复现, 再谈改进
- 新开 `fairchem_unit` 分支, 代码对齐完成
  - Loss: paper 的 `(1/3m)·|F−F*|²` (MSE per scalar component) + Huber/MAE energy 可切
  - element_refs: 切到 TransIP 同款 `omol_elem_refs_dense.yaml` (UMA training_release 版本, 字节一致)
  - 关 `ChemPropEmbedder`, atom embedding 只走 `nn.Embedding(Z, d)`
  - d=384, L=8, H=6 (TransIP-S sizing); fp32; max_atoms=2700; equiv_coef=5
- 启动训练 `fc_transip_strict_*_4M80ep`
