# 2026-05-15

- 训练崩了两次, 均已修复
- **崩溃 1: 多 rank 写入文件冲突**
  - 多 rank 同时写 `OmegaConf.save` / `torch.save` ckpt metadata
  - 根因: timestamp_id 跨 rank 不一致, 各 rank 写到不同子目录的同名文件冲突 (跟 0518 ckpt scatter 是同一类 bug 的早期形态)
  - 临时修: 把所有 disk 写操作 gate 到 `if rank == 0`, 加 `_barrier()` 确保 ckpt 完成后 worker 才推进
- **崩溃 2: CUDA 崩溃** `Tensor [N,3] is at version 3; expected version 2`
  - DDP reducer 跟 `create_graph=True` second-order graph 竞态 (老 PyTorch 已知 bug)
  - 临时改 bf16 mixed precision 绕开 (二阶图小一档, race window 没了)
  - 后续验证新 PyTorch 已修, fp32 现在可以再开
- 继续 strict-alignment 训练
