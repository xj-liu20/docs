# 2026-05-19

> Project: FieldQuanta/predict_molecule · Tags: #eval-result #bug-fix #infra

- Strict baseline 5-epoch ckpt (step 8000) 在 full Val-Comp (2.76M, 8 cat) 做首次对照, vs TransIP-S paper
  - F: **144.5** vs paper 146.6 meV/Å (基本持平, 0.99×)
  - E: **25.3** vs paper 22.3 meV/atom (1.13× worse)
  - 指数拟合外推到 80 epoch: E ≈ 3.1, F ≈ 32.9 (paper 3.99 / 31.90)
- 发现 launcher JOB_TIMESTAMP race bug
  - 症状: `fc_transip_strict_sharedrbf_paper_4M80ep` 32 个 DCP shard 散在 `_052152/` 和 `_055240/` 两个目录, 任一都不可加载
  - 根因: SenseCore master pod 比 worker pod 晚起 30 min, 超过 worker 60s poll timeout, 各 rank 静默落回 `datetime.now()` → 时间戳分裂
  - 修复 (`launchers/fairchem_torchrun.py:88-208`): poll 60s → 30 min, 静默 fallback → `raise RuntimeError`
  - 写入 `ROADMAP.md` 死路 → 基础设施表
- 飞书 CLI 试图打通日报推送
  - 企业 App 161 scope 申请, 管理员只放行 4 个, 关键 `docs:permission.member:create` 缺
  - bot 模式可建文档但 grant 给自己又缺 scope, 个人 App setup 成本太高
  - 改用 GitHub `xj-liu20/docs` repo 写日报 (即本仓库)
- 待办
  - 合并 `_052152/` + `_055240/` 让 5-epoch ckpt 可 resume
  - 当前 launcher 修复单独 commit
  - 等 strict baseline 跑完剩余 ~30 hr → 80-epoch eval
  - 启 Tier 1.1 `equiv_coef=0` ablation (62 hr)
