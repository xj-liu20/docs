# 2026-05-19

> Project: FieldQuanta/predict_molecule · Tags: #eval-result #bug-fix #infra

- Strict baseline 5 epoch ckpt (step 8000) 在 full Val-Comp (2.76M, 8 cat) 第一次对照 TransIP-S paper
  - F: 144.5 vs paper 146.6 meV/Å (0.99×, 基本持平)
  - E: 25.3 vs paper 22.3 meV/atom (1.13×, 还差点)
  - 指数拟合外推 80 epoch: E ≈ 3.1, F ≈ 32.9 (paper 3.99 / 31.90), 大概率追平
- launcher JOB_TIMESTAMP race 撞 ckpt scatter, 折腾一上午找根因
  - 症状: `fc_transip_strict_sharedrbf_paper_4M80ep` 32 个 DCP shard 散在 `_052152/` 和 `_055240/`, 任一都 load 不动
  - 初步以为 master pod 晚 30 min 起, 看 filesystem mtime 发现真实差 < 3 秒 (rendezvous 13:52:54 / DIR_A 13:52:56 / DIR_B 13:52:59), 推翻
  - 真因 3 层叠加:
    1. SenseCore pod 时钟没 NTP 同步, master ≈ 真 UTC, worker 慢 31 分钟
    2. Launch yaml 每 pod 各自 `export JOB_TIMESTAMP=$(date +...)`, env 注入了不同字符串
    3. AFS 跨节点同步 lag, worker 60s poll 内看不到 rank 0 写的 rendezvous, fallback 到自己 env
  - 4 招防御 (commit `4bb20b7`): (a) rank 0 multi-node 下忽略 env 强制 `datetime.now()` (b) worker poll 60s→30min (c) 静默 fallback→`raise RuntimeError` (d) launch 命令删 `export JOB_TIMESTAMP`
  - 同步更新 `ROADMAP.md` 死路表 + `CLAUDE.md` 两处 launch 命令 + launcher docstring
- 飞书 CLI 折腾一圈, 放弃
  - 企业 App 161 scope 申请, 管理员只放行 4 个, 关键 `docs:permission.member:create` 缺
  - bot 模式可建文档但 grant 给自己又缺 scope, 个人 App setup 成本太高
  - 改用 GitHub `xj-liu20/docs` repo 写日报 (即本仓库)
- Throughput 实测 + ROADMAP 算力预算修正
  - 36.56 epoch / 23h 5m 39s → 1759 samples/sec (4M scale, autograd)
  - 比 0518 旧估 1434 sps / 62 hr 跑 80 ep 快 18% (旧估含 ckpt 写入 / DataLoader spawn 一次性开销)
  - 4M × 80ep 新估 50.5 hr, 剩余 ~27.4 hr → 预计 2026-05-20 16:30 完成
  - ROADMAP 三个 scenario 同步修: Minimal 332→274 hr (11.4 天), Standard 642→533 (22 天), Full 1054→881 (37 天)
- TF32 启用 (commit `21aa674`)
  - 一行 `torch.set_float32_matmul_precision('high')` 加到 `launchers/fairchem_torchrun.py:main()` 入口
  - PyTorch 2.6 默认 `'highest'` = 纯 fp32, TensorCore 关; `'high'` 启 TF32 (10-bit 尾数, fp32 accumulator)
  - 实测 force cos_sim=1.0 vs 纯 fp32 (M=1000/2000), 精度损失 ~1e-3 相对值, 远低于 force MAE
  - 当前 strict run 来不及享受 (浪费 ~7 hr), Tier 1.1 起所有 run 自动 -15% wall-time
- 6 个 commit 全推 GitHub (新仓库 `FieldQuantum/predict_molecule`)
  - `4bb20b7` JOB_TIMESTAMP race fix + CLAUDE.md docs catch-up
  - `6f9b3c8` backbone shared-RBF (4× less GPU memory)
  - `3f9840c` paper MSE force loss + full Val-Comp eval config
  - `1fd760b` multi-rank stability fixes (SPAWN / rank0 save_state / err msg)
  - `21aa674` TF32 enable
  - `872c88a` T1.2 三阶段配置 (88M direct → 88M autograd → 4M anneal)
  - `07d940b` T1.2 配置 bug 修复 (direct_forces 嵌套→top-level)
  - 全 7 个 branch (fairchem_unit + main + 5 历史实验分支) 都推上去备份
- T1.2 path A V2 三阶段训练 yaml 预实现 (commit `872c88a`, ROADMAP §Tier 1)
  - 新增 `configs_fc/dataset/omol_88M.yaml` (全集 87.7M train + 8-cat val)
  - 新增 `t1_stage1_direct_88M.yaml`: 88M × 3ep direct-force pretrain, bf16, lr=2e-4, ~8-10h
  - 新增 `t1_stage2_autograd_88M.yaml`: 88M × 1ep autograd calibration, fp32+TF32, lr=1e-4, load stage1.pt, ~13.7h
  - 新增 `t1_stage3_anneal_4M.yaml`: 4M × 5ep LLaMA-3 anneal, lr 5e-5→5e-6, load stage2.pt, ~3h
  - 两个潜在 blocker 实际早就现成: `heads_transformer_direct.py` 有 direct force head, `prefix_unit.py:76` ckpt load 已经 `strict=False`
- dev box smoke 验证 3 项
  - **#1 88M dataset 完整性**: 69 shards / 458 GB / metadata.npz `natoms.shape=(87,687,161,)` = 87.7M samples, 与 OMol25 paper 一致
  - **#2 direct→autograd ckpt 跨阶段 load**: 构造 stage 1/2 HydraModel + 跑实际 `load_state_dict(strict=False)`, 结果 0 missing, 6 unexpected (全是 `output_heads.omol_forces.force_mlp.{0,2,4}.{weight,bias}`, 自动丢), `energy_mlp[0].weight` byte-identical 迁移
  - **#3 bf16 direct + L_eqc 二阶图 stability**: stage 1 yaml 启短 smoke (10 步), `train/loss=385.45 @ step 0` 数值健康, 无 NaN/Inf 也无 DDP "Grad strides" warning (direct 路径不撞老 PyTorch reducer race)
- 顺手修个 canonical_config bug (commit `07d940b`)
  - 报 `Found unused keys: {'direct_forces'}`
  - 根因: 我把 `direct_forces` 写在 `backbone:` 嵌套下, 打断了 parent 的 `${direct_forces}` interpolation 链, 留下孤儿 top-level key
  - 修: 改写到 top-level (跟 parent `omol.yaml:84` 同层)
- 下午 17:11 strict baseline 训练在 step 111226 (epoch 43 of 80, 54%) 撞 `CUDA error: initialization error` 崩溃
  - 就是 commit `1fd760b` 已修过的 FORK + CUDA bug, 但 strict run 0518 启的, 用的还是老 FORK 配置, 修复落地比 run 启动晚了一天
  - 撑了 ~27 hr 才撞, 比之前 fc_transip_paper_4M80ep step 21907 (6h) 撞那次坚持得久
- 准备从 step 110000 ckpt resume (撞前 18 min 刚存的, 只损失 1226 步 ≈ 24 min 训练)
  - 之前 JOB_TIMESTAMP race bug 导致 ckpt 散在 `_052152/` (ranks 1-31) + `_055240/` (rank 0 + .metadata), 任一都加载不动
  - 启动 yaml prepend cp 段把 ranks 1-31 distcp 合并到 `_055240/`, gated NODE_RANK=0, 写 marker 让 worker 等
- 启动踩了 3 个坑, 一个一个解
  - **坑 1: dev box 不能 cp** — 所有 ckpt 文件 root 拥有 (SenseCore pod 容器内 uid 0 跑训练), 我 uid 5019 只读, 没 sudo, 只能把 merge 嵌到 pod 启动 yaml 里跑
  - **坑 2: cp 多行命令被 yaml 解析切掉** — 第一版 `cp -n $SRC/*.distcp $DST/` 里的 `$DST` 在 SenseCore yaml 里丢了, cp 报 "target __9_0.distcp is not a directory"; 换成逐文件 `for f; do cp "$f" "$DST/$base"; done` 全引号才稳
  - **坑 3: wandb api_key not configured** — yaml 漏 `export WANDB_API_KEY=`, fairchem 起来 wandb init 卡死。补上 key, 同一 run_id (`fc_transip_strict_sharedrbf_paper_4M80ep_20260518_055240`) 续写 wandb 历史
- 启起来后发现 fairchem 选错了 resume ckpt, 多损失 10k 步
  - 看 log "Saved dcp checkpoint to step_100000" 反复出现, step_110000 没动
  - 根因: fairchem `get_most_recent_viable_checkpoint_path` 按目录 **mtime** 选 latest, 不是按 step number。我合并顺序 `for step in 110000 100000` 先做 110000 (mtime 17:51) 再做 100000 (mtime 17:54+), step_100000 mtime 反而更新, fairchem 误选
  - 多损失 ~10k 步 ≈ 3.9 epoch ≈ 2.5 hr 训练
- 走 B 方案: 杀掉重启, yaml 加 `touch step_110000` bump mtime 到最新, fairchem 就会选 step_110000 而不是 100000
- Resume 起来后 pull 整个 65 个 eval block 的曲线, 坐实"E 追上但 F 反而 plateau"的观察 (完整数据 + 分析 → `notes/2026-05-19_strict_baseline_eval_curve.md`)
  - 5 ep: F 144.5 / E 25.3, F vs paper 0.99×, E vs paper 1.13× (F 持平, E 差)
  - 42 ep: F 55.8 / E 4.80, F vs paper 1.75×, E vs paper 1.20× (E 几乎追平, F 越拉越远)
  - 最近 10 eval 斜率: F -0.2/eval (plateau), E -0.05/eval (慢但稳)
  - 80 ep 外推: F ~52 (paper 31.9, 追不上), E ~3.85 (paper 3.99, 可能反超)
  - 3 个根因猜测: (1) paper MSE force loss 梯度 ∝ 残差, 自我衰减 plateau, 而 TransIP code 的 L2NormLoss 梯度固定为 1 不会 (2) L_eqc 帮 E (标量) 比帮 F (3-vec) 更多 (3) paper 曲线本身就是早期快后期慢, 我们 5→42 ep 比 paper 平均速率快, 不一定追不上
  - 决定 A 跑完先看 80 ep 终态再说; 如果 F 终态明显 > 32 / paper 还是 32, 再考虑切 `tasks=omol_e_force` (L2NormLoss) 或上 Tier 1.2 V2 path A
- 顺着 F plateau 怀疑往下挖, audit ~/TransIP 7 个分支的 force loss 实现 (详细见 `notes/2026-05-19_transip_loss_audit.md`)
  - 全部 7 个分支 (main/dsornd yaml + arun_main/rbf/rbf_2/rbf_2_dist_rbf_llnl/rbf_edge_index 的 lightning 源码) **一致用 per-atom L2 norm 一阶 + mean over atoms per graph + mean over graphs**, 没一个用 paper text 写的 MSE
  - 代码里还留着注释痕迹 `# e_loss = F.mse_loss(...)` / `# f_loss = torch.mean(torch.sqrt(...))`, 显示作者**试过 MSE 后主动弃用**
  - 加上我们的 plateau 曲线 (MSE 梯度 ∝ 残差自衰减, 锁死在 ~56 meV/Å) — 三项硬证据一致指向 **paper text "L_F = MSE" 是 typo**, paper 31.9 实际是 L2NormLoss 训的
  - 跟 `equiv_coef: 5000 #5000 (paper 说 5)` 同款 stale typo 性质
- 决定切 b 方案: 杀当前 MSE run, 切 `tasks=omol_e_force` (L2NormLoss) resume from step_110000, 新 run dir 跟 MSE run 分开 (MSE run 当对照组, 65 eval block 已存 notes)
- 启动 yaml (b 方案, paper-code-aligned L2NormLoss)
  ```bash
  export WANDB_API_KEY=<key>
  export PYTHONPATH=/mnt/afs/home/liuxiangjia/predict_molecule/src
  if [ "$SENSECORE_PYTORCH_NODE_RANK" = "0" ]; then
    touch /mnt/afs/share/checkpoint/predict_molecule/liuxiangjia/fc_transip_strict_sharedrbf_paper_4M80ep_20260518_055240/checkpoints/step_110000
    sleep 2
  fi
  torchrun --nproc_per_node $SENSECORE_ACCELERATE_DEVICE_COUNT --nnodes $SENSECORE_PYTORCH_NNODES \
      --node_rank $SENSECORE_PYTORCH_NODE_RANK --master_addr $MASTER_ADDR --master_port $MASTER_PORT \
      /mnt/afs/home/liuxiangjia/predict_molecule/src/predict_molecule/launchers/fairchem_torchrun.py \
      -c /mnt/afs/home/liuxiangjia/predict_molecule/configs_fc/omol.yaml \
      cluster=sensecore tasks=omol_e_force \
      element_refs=omol_elem_refs_dense backbone.use_chem_embed=false backbone.use_distance_bias=false \
      +backbone.use_shared_rbf_bias=true +backbone.shared_rbf_num_channels=64 +backbone.shared_rbf_max_distance=10.0 \
      +job.timestamp_id=fc_transip_codeloss_sharedrbf_4M80ep_l2norm_resume_20260518_055240 \
      job.run_name=fc_transip_codeloss_sharedrbf_4M80ep_l2norm_resume
  ```
  - 关键: `tasks=omol_e_force` (不是 `omol_e_force_paper`), `+job.timestamp_id=...l2norm_resume...` 开新 run dir 避免覆盖 MSE run
  - fairchem ckpt load 走 `strict=False`, energy_mlp/force_mlp/ backbone weights 全继承, optimizer/scheduler 从 step_110000 续上 (跟 MSE run 一致)
  - 预期: L2 norm 梯度幅度固定 1, 不 plateau, 剩 38 epoch 把 F 从 55.8 → paper 31.9 量级有戏
- L2NormLoss resume 跑了 ~2 hr / 6 epoch, **F 急剧加速下降, 验证 paper text MSE 是 typo 假说成立**
  - resume 起点 step_110000 (epoch 42, F=55.76 / E=4.80) → 现在 step ~125800 (epoch ~48, F=48.12 / E=4.48)
  - 6 epoch F 降 -7.6 meV/Å = **-1.3 / epoch**, MSE 最后 10 eval 斜率 -0.1/epoch (plateau), **L2 norm ~13× 快**
  - E 也稳步下降, 没受切 loss 影响 (-0.05/epoch)
  - 80 ep 朴素外推 F 15.7 meV/Å (反超 paper 31.9), 现实考虑自然 plateau 估终态 30-35, 即追平 paper
  - 决策门 A 提前实质过了: paper 31.9 用 L2NormLoss 训得出, MSE 是 typo
- 待办
  - 等 L2NormLoss resume run 跑完剩余 ~21 hr (32 epoch × 37.9 min) → 80-epoch eval 实锤 paper 对齐
  - ROADMAP / CLAUDE.md 同步 update — TransIP strict-align knobs 段把 default 切回 `tasks=omol_e_force` (L2NormLoss), 死路表加 "paper text MSE 是 typo" 一行
  - 启 Tier 1.1 `equiv_coef=0` ablation (50.5 hr, 含 TF32 加速)
