# 2026-05-19

> Project: FieldQuanta/predict_molecule · Tags: #eval-result #bug-fix #infra

- Strict baseline 5-epoch ckpt (step 8000) 在 full Val-Comp (2.76M, 8 cat) 做首次对照, vs TransIP-S paper
  - F: **144.5** vs paper 146.6 meV/Å (基本持平, 0.99×)
  - E: **25.3** vs paper 22.3 meV/atom (1.13× worse)
  - 指数拟合外推到 80 epoch: E ≈ 3.1, F ≈ 32.9 (paper 3.99 / 31.90)
- 发现 launcher JOB_TIMESTAMP ckpt scatter bug, root cause 比预想复杂
  - 症状: `fc_transip_strict_sharedrbf_paper_4M80ep` 32 个 DCP shard 散在 `_052152/` 和 `_055240/` 两个目录, 任一都不可加载
  - **初步诊断错**: 我先猜"master pod 晚 30 min 起", 用户质疑 ("从系统日志看似乎只差一分钟")
  - **真实根因 3 层** (filesystem mtime 实测 rendezvous 13:52:54 / DIR_A 13:52:56 / DIR_B 13:52:59, 真实差 < 3 秒, 30 min 差全在 `datetime.now()` 字符串里):
    1. SenseCore pod 时钟未 NTP 同步, master ≈ 真 UTC, workers 慢 31 分钟
    2. Launch yaml 每 pod 各自 `export JOB_TIMESTAMP=$(date +...)`, env 注入不同字符串
    3. AFS 跨节点同步 lag, worker 60s poll 看不到 rank 0 写的 rendezvous, fallback 到自己 env
  - **修复 4 招**: (a) rank 0 multi-node 下忽略 env 强制 `datetime.now()` (b) worker poll 60s→30min (c) 静默 fallback→`raise` (d) launch 命令删 `export JOB_TIMESTAMP`
  - 写入 `ROADMAP.md` 死路表 + `CLAUDE.md` 两处 launch 命令 + launcher docstring
- 飞书 CLI 试图打通日报推送
  - 企业 App 161 scope 申请, 管理员只放行 4 个, 关键 `docs:permission.member:create` 缺
  - bot 模式可建文档但 grant 给自己又缺 scope, 个人 App setup 成本太高
  - 改用 GitHub `xj-liu20/docs` repo 写日报 (即本仓库)
- Throughput 实测 + ROADMAP 算力预算修正
  - 36.56 epoch / 23h 5m 39s → **1759 samples/sec** (4M scale, autograd)
  - 比 2026-05-18 旧估 (1434 sps / 62 hr 跑 80 ep) 快 18%, 旧估含 ckpt 写入 / DataLoader spawn 一次性开销
  - 4M × 80ep 新估 **50.5 hr** (旧 62), 剩余 **~27.4 hr → 预计 2026-05-20 16:30 完成**
  - ROADMAP 三个预算场景同步修正: Minimal 332 hr→274 hr (11.4 天), Standard 642→533 (22 天), Full 1054→881 (37 天)
- TF32 启用 (commit `21aa674`)
  - `torch.set_float32_matmul_precision('high')` 加到 `launchers/fairchem_torchrun.py:main()` 入口
  - PyTorch 2.6 默认 `'highest'` = 纯 fp32, TensorCore 关; `'high'` 启用 TF32 (10-bit 尾数, fp32 accumulator)
  - 实测 force cos_sim=1.0 vs 纯 fp32 (M=1000/2000), 精度损失约 1e-3 相对值, 远低于 force MAE
  - 当前 strict run 来不及享受 (浪费 ~7 hr), Tier 1.1 起所有 run 自动 -15% wall-time
- 本地 commit 累积 + 推 GitHub `FieldQuantum/predict_molecule`
  - `4bb20b7` JOB_TIMESTAMP race fix + CLAUDE.md docs catch-up
  - `6f9b3c8` backbone shared-RBF (4× less GPU memory)
  - `3f9840c` paper MSE force loss + full Val-Comp eval config
  - `1fd760b` multi-rank stability fixes (SPAWN / rank0 save_state / err msg)
  - `21aa674` TF32 enable
  - `872c88a` T1.2 三阶段配置 (88M direct → 88M autograd → 4M anneal)
  - 全 7 个 branch 都 push 到 GitHub `FieldQuantum/predict_molecule` 备份
- T1.2 path A V2 三阶段训练 yaml 实施 (commit `872c88a`)
  - 新增 `configs_fc/dataset/omol_88M.yaml` (88M 全集 train + 8-cat val)
  - 新增 `t1_stage1_direct_88M.yaml`: 88M × 3ep direct-force pretrain, bf16, lr=2e-4, ~8-10h
  - 新增 `t1_stage2_autograd_88M.yaml`: 88M × 1ep autograd calibration, fp32+TF32, lr=1e-4, load stage1.pt, ~13.7h
  - 新增 `t1_stage3_anneal_4M.yaml`: 4M × 5ep LLaMA-3 anneal, lr 5e-5→5e-6, load stage2.pt, ~3h
- 关键 prerequisite 验证 (用户提到的两个潜在 blocker, 实际已经现成)
  - **direct force head 已实现**: `transip/models/heads_transformer_direct.py` 有 `TransformerEnergyOnlyHead` + `TransformerDirectForcesHead`; `AtomTransformerBackbone` 真实 kwarg `direct_forces` (默认 True) 验证通过
  - **不匹配 ckpt 读取已实现**: `prefix_unit.py:76` `load_state_dict(state_dict, strict=False)`, stage 1 → stage 2 head 切换时 `force_mlp` 自动 unexpected (丢弃), `energy_mlp` 参数名一致 (无缝迁移)
  - smoke 验证: 3 stage hydra resolve + backbone/heads instantiation 全过
- 待办
  - 合并 `_052152/` + `_055240/` 让 5-epoch ckpt 可 resume
  - 等 strict baseline 跑完剩余 ~27 hr (ETA 2026-05-20 16:30) → 80-epoch eval
  - 启 Tier 1.1 `equiv_coef=0` ablation (50.5 hr, 含 TF32 加速)
