# 2026-05-20

> Project: FieldQuanta/predict_molecule · Tags: #eval-result #paper-align #infra #decision

- L2NormLoss resume run 跑完 80 epoch (step 207800, final/ ckpt 落盘), per-cat 跟 paper Table 1b 严格对比, 4 cat 全部没追上
  - bio E=2.20 (vs 2.02, 1.09×) F=42.70 (vs 29.00, 1.47×)
  - ely E=3.80 (vs 3.22, 1.18×) F=40.90 (vs 30.31, 1.35×)
  - metal E=7.40 (vs 5.71, 1.30×) F=68.30 (vs 59.28, 1.15×)
  - NeutOrg E=7.60 (vs 5.38, 1.41×) F=71.90 (vs 51.91, 1.39×)
  - paper Table 1b "Total" 列 (E=3.99 F=31.90) 用 full Val-Comp 2M 真实分布加权, 我们 eval sample_n=20k 平衡子集对不上, 只能 per-cat 对照
- alignment audit 跟 paper / TransIP 代码逐项核, 剩余差异只有 max_atoms 2700 vs paper yaml 10000, normalizer_rmsd 1.4336 vs 1.423 (0.7%), shared_rbf vs literal use_rbf_bias (拓扑等价 expand 共享)
  - max_atoms 想拉但 GPU 已 66% (53/80 GB) at M=2700, M² scaling 拉到 4000 就 OOM, 这条堵死
  - force coef 全程对齐, omol_e_force.yaml hardcoded coefficient=15 跟 TransIP omol.yaml force_coef=15 一致, omol_e_force_paper.yaml 用 coefficient=5 (吸收 paper MSE 公式的 1/3 因子), 切换时跟着 task yaml 走没 bug
- F gap 大概率 root cause: ep 42 才切 L2NormLoss, 前 42 epoch 用 MSE 拖累了 backbone
  - 切换瞬时 force per-component grad 从 ~0.33 (MSE × 5 × 2/3) 跳到 ~8.7 (L2 × 15/√3), 26× 跳变
  - E mae 同步暴跌 (5.92 → 7.57, +28%), 4000 step 内恢复, 之后 35 epoch 慢漂 5.98→5.30 (-0.019/ep), lr 衰减拖累, 走不出 MSE-trained basin
  - 易 cat 改善大 (bio 3.50→2.20, -37%), 难 cat 几乎不动 (metal 7.60→7.50, -1%), 共享 backbone 表征 coupling 实锤
  - train/loss PRE 50 步 max=0.295, POST 50 步 max=2.139, 跳 10× 但远低于 clip=200, 排除 clip squeeze E gradient 这条假设
  - fairchem TrainEvalRunner 不显式 log grad_norm 到 wandb, 严格 verify 要给 trainer 加 hook
- 早期 MSE phase 还存在 force MAE 大震荡, 之前没注意
  - ep 0-10 内 F_4mean 反弹 3 次, 135→264→126→158→114→178→146→108, 振幅 100-265
  - E_4mean 同步更夸张, 17→301→17→35→22→15→30, 振幅 15-300 (20×)
  - train/loss step [30k, 60k) max/mean = 7.8× (post-switch L2 段是 1.7×), MSE outlier batch driving gradient spike
  - 量化: OMol25 长尾 |F| ~50 eV/Å normalized residual ~35, MSE per-component grad ≈ 35×5×(2/3) ≈ 117 vs L2NormLoss = 15/√3 ≈ 8.7, MSE outlier grad 是 L2 的 13.4×, 单 batch 就能把 backbone 推到下个 basin
  - 跟 2025-11 月 Huber 替换 squared 时同款 issue, 这是 paper text "L_F = MSE" 是 typo 的第三个硬证据
- torch.compile smoke 测了一轮 (scripts/compile_smoke.py), 三个 backend 全堵死
  - inductor: 系统缺 /usr/include/python3.10/Python.h, Triton 编译 CUDA glue 报 fatal error, apt 装 python3.10-dev 需 sudo
  - aot_eager: PyTorch 上游 "does not currently support double backward", 我们 create_graph=True autograd forces 必需双反向, 用不了
  - eager (纯 dynamo retrace): 跑通, 数值 byte-identical (bb.eval() 后), 但无 fusion 反而 1.55× slower, 没价值
  - 结论: 当前 cluster 镜像 + PyTorch 2.6 组合下 torch.compile 没可用 speedup 路径, 后面 run 不带; ROADMAP Tier 2 item 8 (torch.compile) 同步划线搁置, 死路表加一行, 重启条件: cluster admin 装 python3.10-dev 或换 image (inductor) / PyTorch 上游加 aot_autograd double-backward 支持; Tier 1.2 V2 stage 1 direct_forces=true 可单独试 aot_eager
- 启 from-scratch L2 80 epoch run 验证 backbone 全程在 paper L2-gradient regime 下能不能追到 paper, run_dir `fc_transip_strict_sharedrbf_L2scratch_4M80ep_20260520_051029`
  - 跟 resume run 唯一区别: F loss ep 0-80 全程 L2NormLoss, TF32 自动开 (commit `21aa674` 已 merge), 跳过 25k step MSE 早期震荡 + 不需要 ep 42 大调整
  - throughput 实测: 2h 50m 52s / 6.20 epoch → 2419 sps, 80 ep = 37 hr, 比 0519 估的 50.5 hr 快 37%; 来源 TF32 (~15% microbench) + 纯 from-scratch (无 resume ckpt 反复写 / 多 segment wandb / DataLoader 多次 spawn) ~22%
  - 80 ep 新 ETA ~37h on 32 GPU, 2026-05-21 ~18:00 (从 05-20 05:10 起)
  - ROADMAP 算力预算同步重算: T0/T1.1 各 37 hr (was 50.5), T1.2 24 hr (was 25), T2 118 hr (was 163), 三个 scenario Minimal 200 hr (~8.3 天), Standard 389 hr (~16.2 天), Full 642 hr (~26.8 天), 都比之前估的低 27%
  - 5 ep 中期 eval (step 12800, ep 4.97) 跟 paper Table 1a TransIP-S 5 ep per-cat 对比, 8 个 metric (4 cat × E/F) 全部低于 paper, ratio 0.41-0.89×
    - F: ely 68.3 (paper 129.6, 0.53×), bio 73.6 (181.1, 0.41×), metal 102.1 (132.5, 0.77×), NeutOrg 110.8 (165.0, 0.67×)
    - E: ely 12.3 (paper 15.9, 0.77×), bio 7.6 (17.3, 0.44×), metal 16.4 (18.5, 0.89×), NeutOrg 17.9 (23.5, 0.76×)
    - 旧 resume run 5 ep total F=144.5/E=25.3 是跟 paper 146.6/22.3 持平; 新 run 4-cat 简单 mean F=88.7/E=13.6, 直接 BEAT paper ~40%, "MSE 早期震荡 + 切换创伤"的训练浪费完全消除, backbone 跟 paper basin 同 path 假说基本实锤
  - 启动命令
  ```bash
  export PYTHONPATH=/mnt/afs/home/liuxiangjia/predict_molecule/src
  torchrun --nproc_per_node $SENSECORE_ACCELERATE_DEVICE_COUNT --nnodes $SENSECORE_PYTORCH_NNODES \
      --node_rank $SENSECORE_PYTORCH_NODE_RANK --master_addr $MASTER_ADDR --master_port $MASTER_PORT \
      /mnt/afs/home/liuxiangjia/predict_molecule/src/predict_molecule/launchers/fairchem_torchrun.py \
      -c /mnt/afs/home/liuxiangjia/predict_molecule/configs_fc/omol.yaml \
      cluster=sensecore element_refs=omol_elem_refs_dense \
      backbone.use_chem_embed=false backbone.use_distance_bias=false \
      +backbone.use_shared_rbf_bias=true +backbone.shared_rbf_num_channels=64 +backbone.shared_rbf_max_distance=10.0 \
      job.run_name=fc_transip_strict_sharedrbf_L2scratch_4M80ep
  ```
- 待办
  - 等 from-scratch L2 run 跑完 (ETA 2026-05-22) 看终态, 跟 resume run 比 backbone 是否摆脱 MSE basin
  - 联系 cluster admin 装 python3.10-dev 或换 image, 后续 inductor 可用
  - Tier 1.2 V2 stage 1 direct_forces=true 可单独试 aot_eager (direct 不需 create_graph)
