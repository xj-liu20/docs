# 2026-05-20

> Project: FieldQuanta/predict_molecule · Tags: #eval-result #paper-align #infra #decision

- L2NormLoss resume run 拉了 step 205200 (epoch ~78.9, 剩 ~3-5 hr 收尾) 的完整 trajectory, 4-cat val 数字接近终态
  - electrolytes: F=41.0 meV/Å / E=3.80 meV/atom / cos=0.960
  - biomolecules: F=42.7 / E=2.20 / cos=0.988
  - metal_complexes: F=68.3 / E=7.50 / cos=0.860
  - neutralorganics: F=71.9 / E=7.70 / cos=0.953
  - reactivity: F=77.3 / E=7.30 / cos=0.973 (paper Table 1b 不报)
  - spice: F=37.1 / E=2.30 / cos=0.982 (paper Table 1b 不报)
- **校正昨天 4-cat 加权 bug** — 之前我用 `electrolytes 67.2 / bio 24.0 / metal 7.6 / neutorg 1.1` 算 4-cat 是错的, 多重错误:
  - **漏 cat**: 之前只 grep 5 cat (electrolytes/metal/neutorg/reactivity/spice), 把 reactivity 当 bio 算; 实际 `canonical_config.yaml` 有 6 cat, biomolecules 单独存在, 我第一轮 grep 时硬编码 cat 列表里没写 `biomolecules` 直接跳过
  - **权重不对**: paper Table 1b "Total" 列是 full Val-Comp 2M (~2.76M samples) 跑出来的, 不是 4-cat 加权。我用的 67/24/7.6/1.1 权重算出 paper E 4-cat= 3.14 ≠ paper Total 3.99, 反推 paper Total 必然按 full val 真实 cat 分布加权 (elytes 主导, 但还掺 reactivity/spice 等), 不是 4-cat
  - 我们 eval 用 sample_n=20k 平衡子集, 严格说不能直接报 "Total" 对照 paper, 只能 per-cat 对比
- Pull TransIP paper Table 1b verbatim (arxiv 2510.00027 v3, page 9), per-cat 严格对比
  - TransIP-S 80ep: bio E=2.02 F=29.00 / ely E=3.22 F=30.31 / metal E=5.71 F=59.28 / NeutOrg E=5.38 F=51.91, Total E=3.99 F=31.90
  - 我们 step 205200 (ep 78.9): bio E=2.20 F=42.70 / ely E=3.80 F=41.00 / metal E=7.50 F=68.30 / NeutOrg E=7.70 F=71.90
  - **per-cat F gap: bio 1.47× / ely 1.35× / metal 1.15× / NeutOrg 1.39×**
  - **per-cat E gap: bio 1.09× / ely 1.18× / metal 1.31× / NeutOrg 1.43×**
  - **4 个 cat 全部 E/F 都比 paper 高**, 没有任何 cat 超 paper — 排查过程中先用错误 weighting (67/24/7.6/1.1) 算出 4-cat E=3.74 误以为超过 paper Total 3.99, 校正后撤回
- Paper alignment 系统审计 (vs ours strict run)
  - 已对齐: TransIP-S sizing (d=384/L=8/H=6), MLP coord embedder, qk_norm/proj_drop/qkv_bias, chem_embed off, force loss L2NormLoss (post-resume), element_refs dense byte-identical, optimizer (AdamW lr 5e-4 wd 1e-3), schedule (cosine warmup 1% → lr_min 1%), coef E=5/F=15/eqc=5 (paper 文本; TransIP yaml 5000 typo), 32 GPU × 80 ep, fp32, clip_grad_norm=200, direct_forces=false
  - 剩余差异: (1) max_atoms 2700 vs TransIP yaml 10000 (3.7× 小), (2) shared_rbf vs literal use_rbf_bias 拓扑等价但 expand 共享, (3) normalizer_rmsd 1.4336 vs 1.423 (0.7%)
- 首轮怀疑 max_atoms (effective batch 27% paper) 是 force gap 主因 — **被用户打掉**
  - 当前 strict run 单卡 GPU 内存 66% (~53 GB / 80 GB) at M=2700
  - 按 M² scaling 估 M=4000 → ~116 GB OOM, M=3500 → ~89 GB OOM, M=6000 完全不可能
  - paper 同 80 GB GPU 跑 max_atoms=10000 这条疑点先搁置 (可能 paper 有别的内存优化, 或 effective batch 也常被 mol size 分布卡到 <10000)
- 用户进一步 catch: force coef 跟着 task yaml 变是否对齐?
  - 我们 yaml 设计: `omol_e_force_paper.yaml` (paper text MSE, 现弃用) hardcoded `coefficient: 5.0 = 15/3` 吸收 1/3 因子; `omol_e_force.yaml` (TransIP code L2NormLoss, 现用) hardcoded `coefficient: 15`
  - canonical_config.yaml 实跑值 verify: E PerAtomMAELoss coef=5 / F L2NormLoss coef=15 / equiv MAELoss coef=5 — **完全对齐 paper / TransIP code, 无 bug**
- 切换瞬时 force per-component gradient 量级变化估算
  - MSE 阶段: grad ∝ Δ × 5 × (2/3) ≈ 0.33 (Δ~0.1 eV/Å typical)
  - L2NormLoss 阶段: grad = 15/√3 ≈ 8.7
  - **force gradient 切换后 effective 跳 ~26×**, 解释 6 epoch 加速段; 之后 lr 衰减进入 plateau
- 切换前后 E mae 跨切换 trajectory + train/loss 对比, 验证 backbone 耦合假说
  - PRE 最后 5 eval E_4mean: 6.12/6.17/6.03/5.90/**5.92** (慢降, plateau)
  - 切换瞬时 (step 112400, 切后 1600 step) E_4mean = **7.57 (+28% 暴跌)** ← E 立刻退化, 共享 backbone 被 force loss 切换搅乱
  - POST 4000 step 内恢复 (step 114800 E=6.33), 之后 35 epoch 慢漂 5.98 → 5.30 (-0.68 总, ~ -0.019/epoch, lr 衰减拖累)
  - cat-specific: 易 cat 改善大 (bio 3.50→2.20, -37%), 难 cat 几乎不改善 (metal 7.60→7.50, -1%)
  - train/loss PRE last 50 步 mean=0.158 / max=0.295; POST first 50 步 mean=1.560 / max=**2.139** — 切换瞬时 loss ×10, 但 max 2.14 远低于 clip_grad_norm=200, **clip 全程没触发, 排除"clip squeeze E gradient"假设**
  - fairchem TrainEvalRunner 不显式 log grad_norm 到 wandb (只有 loss/lr/step/throughput), 严格 verify 需要给 trainer 加 hook 或读 ProfilerCallback trace
- 用户进一步 catch: 早期 MSE 是否导致 force MAE 震荡?
  - 早期 15 个 eval (step 2400-25600, ep 0-10) F_4mean: 392→167→164→**135**→**264**→222→126→**158**→141→114→**178**→146→108→102→101 — **ep 3-10 在 100-265 之间反弹 3 次**
  - E_4mean 同步震荡: 495→56→50→**17**→**301**→213→17→35→22→15→**30**→21→17→15→15 — E 振幅更夸张 (15→301, **20×**)
  - train/loss spike stats: step [30k, 60k) max=2.87 / mean=0.37, **max/mean = 7.8×** (MSE outlier batch driving gradient spike); 同区段 L2 post-switch max=2.21 / mean=1.32 = 1.7× 远更稳定
  - 量化原因: OMol25 长尾 |F| ~50 eV/Å, normalized residual ~35; MSE per-component grad = 35×5×(2/3) ≈ **117** vs L2NormLoss = 15/√3 ≈ **8.7**, **MSE outlier grad 是 L2 的 13.4×**, 单 outlier batch 就把 backbone 推到下个 basin
  - 跟 2025-11 月 Huber 替换 squared 时同款 issue (CLAUDE.md "Problem solved" 段, |r|~50-70 eV/atom 撞 grad_norm 80k)
  - **结论**: paper text MSE 不是 typo 这件事第三个硬证据 — 真用 MSE 早期 25k step 都在 outlier-driven backbone 震荡里, paper 不会拿这种轨迹训出 31.9
- torch.compile smoke 测试 (script `scripts/compile_smoke.py`), 14M-param backbone TransIP-S strict setup
  - **inductor backend (真正能 op fusion)**: 失败 — 系统缺 `/usr/include/python3.10/Python.h` (apt 装 `python3.10-dev` 需 sudo, 没权限), Triton 生成的 CUDA glue C 代码编译报 `fatal error: Python.h: No such file or directory`
  - **aot_eager backend (中等 AOTAutograd 收益)**: 失败 — `RuntimeError: torch.compile with aot_autograd does not currently support double backward`, 我们 `create_graph=True` autograd forces 必需 double-backward, PyTorch 上游限制
  - **eager backend (纯 dynamo retrace)**: 通过, 数值 **byte-identical** (loss_rel = 0, F rel L2 = 0, 设 `bb.eval()` 消除 dropout RNG), 但 wall-time **1.55× slower** (5.65s vs 3.66s for 6 step), 无 fusion 补偿
  - **结论**: torch.compile 在我们当前 (PyTorch 2.6 + autograd forces + cluster 镜像) 组合下无可用 speedup 路径; from-scratch L2 重跑去掉 compile 计划
- 决定启动 from-scratch L2 80 epoch run (验证 backbone 全程在 paper L2-gradient regime 下能否追到 paper)
  - 跟当前 resume run 唯一区别: F loss ep 0-80 全程 L2NormLoss (现 run 是 ep 0-42 MSE + ep 42-80 L2), TF32 自动开 (commit `21aa674`, 现 run 启动时没 merge)
  - 预期: 跳过 25k step MSE 早期震荡, backbone trajectory 跟 paper basin 同 path, 不需要 ep 42 大调整, F/E 终态应该明显优于 resume run (假设 backbone-coupling 假说成立)
  - ETA: ~46h on 32 GPU (TF32 +15% 加速), 2026-05-22 ~17:00 完成
  - 启动命令:
  ```bash
  export PYTHONPATH=/mnt/afs/home/liuxiangjia/predict_molecule/src
  torchrun --nproc_per_node $SENSECORE_ACCELERATE_DEVICE_COUNT --nnodes $SENSECORE_PYTORCH_NNODES \
      --node_rank $SENSECORE_PYTORCH_NODE_RANK --master_addr $MASTER_ADDR --master_port $MASTER_PORT \
      /mnt/afs/home/liuxiangjia/predict_molecule/src/predict_molecule/launchers/fairchem_torchrun.py \
      -c /mnt/afs/home/liuxiangjia/predict_molecule/configs_fc/omol.yaml \
      cluster=sensecore element_refs=omol_elem_refs_dense \
      backbone.use_chem_embed=false backbone.use_distance_bias=false \
      +backbone.use_shared_rbf_bias=true +backbone.shared_rbf_num_channels=64 +backbone.shared_rbf_max_distance=10.0 \
      job.run_name=fc_transip_strict_sharedrbf_L2scratch_4M80ep
  ```
  - 关键: 不 export JOB_TIMESTAMP (fairchem_torchrun.py 在 rank 0 mint + AFS rendezvous 广播, 防 SenseCore pod 时钟漂); run_name 不同自动开新 run_dir, 不会 resume 旧 MSE-phase ckpt; default `tasks=omol_e_force` (commit `1d2a5e3` 切完) 不需要 override
- 待办
  - 等 L2NormLoss resume run 收尾 (~3-5 hr) 拿 80 ep 终态正式数字, 跟 paper per-cat 写 close-up notes
  - 启 from-scratch L2 run (本机 ETA 等 SenseCore 调度排队)
  - 联系 cluster admin 装 `python3.10-dev` (or 换 image), 让后续 inductor 可用
  - Tier 1.2 V2 stage 1 direct_forces=true 阶段可单独试 aot_eager (direct force 不需 create_graph, 上游限制不触发)
