# 2026-05-25

> Project: FieldQuanta/predict_molecule · Tags: #eval-result #paper-align #decision #bug-fix #infra

- T1.1 ablation (eqc=0) 跑完 ep 79.94, 同 ep apples-to-apples 对比 T0 baseline (eqc=5, ep 69.93 crash 终态): F 全 4 cat T1.1 LOW 1.9-3.2 meV/Å (ely 35.0→32.7, bio 35.7→32.5, met 62.5→60.6, NeutOrg 62.5→60.5), E 持平或略好 (-0.4 ~ +0.1)。L_eqc 关闭决策 80 ep 终态实证, equiv_pair=true rotation augmentation 才是学等变性的真机制, L_eqc loss 项冗余, 后续 run 全按 equiv_coef=0 跑
- T1.1 终态 vs paper 80ep per-cat gap: metal F 1.02× (基本追平, 难度最高 cat 反而先追上), bio E 1.04× (基本追平), 其他 F 1.08-1.16× / E 1.12-1.30×。全 cat F gap 1.02-1.16× 比 T0 终态 1.05-1.23× 全面收窄
- 发现 DCP ckpt 严重 silent loss bug: 扫了 13 个 run, EVERY 长 run 缺 2-18 个 shard, 同一 run 同样的 rank 一直缺。例: T0 缺 18/32 shard, T1.1 缺 16+29, stage1 (t1s1_direct_88M_3ep) 缺 24+28; 所有 ckpt 都不可 load。Root cause: PyTorch DCP per-rank write 在 quarkfs AFS 上 open/write/fsync 都返回 success, 但 cross-pod sync 静默 drop 文件; PyTorch all_reduce 看不到 exception, fairchem log "Saved" 成功, 永久数据丢失。不是 code bug, 是 cluster infra
- Fix: prefix_unit.save_state 改成 master rank 单 writer torch.save 单文件 state.pt (DDP 所有 rank state 完全相同, 不需要 dedup sharding 也不需要 collective)。绕开 multi-writer race, 单 writer 不可能有 partial save。Load 优先读 state.pt, fallback DCP 兼容 legacy ckpt (commit `6529476`)。chain scripts (run_t1v2_chain*.sh) 同步改成 symlink state.pt, dcp_to_torch_save 转换不再需要
- 端到端验证: stage 1 smoke 2 step → 写 state.pt (252 MB, 含 model/optim/progress/scheduler/ema 全 keys) → stage 2 smoke 加载 → step 0/1 loss 394.30/531.57 跟旧 DCP 路径 byte-identical, state 完整无损
- t1s1_direct_88M_3ep 旧 ckpt 数据丢失不可恢复, stage 1 要重跑 (现有 launcher 已带新 single-writer save, 重跑出来的 ckpt 安全)
- Stage 1 chain script 启动撞 quarkfs 又一个静默坑: 第一次启用 bash 层 AFS 文件做 master/worker TS rendezvous, pod 3 等 5 min 看不到 master 写的 `_chain_ts_*.txt` 直接 timeout fail。同款 quarkfs cross-pod 元数据 cache stale 问题, 拉长 timeout 是 band-aid 不是 fix
- Chain script 重写: 去掉 bash 层 TS 协调, 改成"每 pod 独立调 torchrun, launcher 内部 TCPStore JOB_TIMESTAMP 协调 run_dir, 阶段完成后各 pod 独立 glob 最新 run_dir 拿 state.pt"。AFS 只用于 hours 前写的文件 read, 不存在跨 pod 立即 read-after-write 操作 (commit `d300a1e`)
- 重启 stage 1 chain 跑起来后扫 run_dir 发现 dual-rundir 现象: 今天 19:35 新 chain 启动同时存在 `_20260524_113529` (新, 正确) 和 `_20260523_062616` (旧, 昨天 ts 被某 pod 当今天 ts 用了, 写了 config 但没训练)。Root cause 同前: launcher 的 `_broadcast_job_timestamp` 也是 AFS sync_file 路径 (master 写, worker 读), worker 在 master 写后 1 sec 读到 quarkfs 缓存里 yesterday 的内容
- Launcher 也修了: `_broadcast_job_timestamp` 改用 `dist.init_process_group(env://, gloo) + broadcast_object_list + destroy_process_group`, 复用 torchrun 已建好的 TCPStore (MASTER_ADDR:MASTER_PORT, 已知 work, SenseCore 网络允许)。4-rank local mp smoke 验证全 rank agree on 同 ts (commit `d2a37e1`)
- 当前 stage 1 仍在跑 (NEW dir, step ~1000+), 单 writer save 保护下 state.pt 仍只落 master 那条路径 (master 用 fresh ts → NEW), OLD dir 只是 config 写错位但无训练影响。stage 1 跑完后 chain 自动接 stage 2/3, 用新 launcher (TCPStore 修过) 不会再有 dual-rundir
- 跑 stage 1 中观察 wandb system_memory_utilization 单调上升, GPU util 100% RAM 时下跌, 每 val event 漏 100-500 MB。Root cause: fairchem `dataloader_builder.get_dataloader` 没传 `persistent_workers`, 默认 False → 每 epoch + 每 val 边界 32 worker/pod 销毁 + 重 spawn, SPAWN context 下 LMDB mmap pagecache / pinned memory / Python heap 不被 OS 立即 reclaim, 累积。每 5000 step 一次 val, 30+ 次 val 后撑爆 pod RAM
- Fix: launcher main() monkey-patch `dataloader_builder.get_dataloader` 加 `persistent_workers=(num_workers > 0)`, worker spawn 一次后留着复用, 不再重 spawn (commit `d032809`)。同时 prefix_unit.save_state 的 "Saved single-file" log 行移进 `is_master` gate 减少噪音 (commit `4cd9238`)。当前 stage 1 不享受 (启动时新 patch 未 merge), stage 2/3 chain 启用新 launcher 自动应用
- 讨论 transip 代码 vendor 现状: 已经在 `src/transip/` (commit 0a41359 from 2026-05-14), 11 个文件里 `prefix_unit.py` (+348 行) / `shims.py` (+63 行) / `loss.py` (新增) 重度自有改动, `transformer.py` / `transformer_core.py` / `heads_*` 等基本 unchanged。决定先维持现状不整合 (改 yaml + sys.modules alias 处理旧 ckpt 兼容性成本不值), 等 Tier 1.2 path A 真出产 (决策门 B 通过) 或 Tier 5.M scale up 真 win 之后再整合成单一 package 发布
- 设计 multi-fidelity Δ-learning 数据路线写进 ROADMAP Tier 3.M, 思路: OMol 大规模结构 + 低精度 xTB label pretrain → 加 head_delta 在 DFT 高精度 fine-tune, `E_pred = head_xtb(h) + head_delta(h)`, autograd 自动拆 force sum 推理 0 overhead。 关键 init/freeze: head_delta 末层 zero-init 保 step 0 退化成 stage 1 模型不跑飞, head_xtb 冻保留 "xtb head ↔ xtb 物理" 归纳偏置, backbone 0.1× LR 防 head_xtb 输出漂走。 工具选 tblite (GFN2-xTB, ASE Calculator, Z=1-86 + 带电 + 开壳 OMol 全兼容), skip UFF/MMFF (平衡几何错 + 不能处理金属/openshell, 跟 OMol 关键 cat 直接冲突), skip DFTB+ (慢 + 参数集麻烦)
- T3.M 关键 framing: 这条路不只是 4M 上榨更多精度, 更重要的是数据路线验证。 结构固定 OMol 88M 跟 T1.2 V2 完全一致, 唯一变量是 label 精度 (Meta 给的 DFT vs 我们自产 tblite xTB, 后者 ~600 CPU-day / 88M 可控)。 若终态 Val-Comp ≈ T1.2 V2 (±5%) → 88M DFT label 不必需, 数据天花板从 OMol 88M (Meta DFT 算力上限) 抬到 ~1B 量级 (PubChem 100M + ZINC + ChEMBL + COD), 这条 trade-off 战略价值远大于直接 MAE 改善。 触发顺序: 等 Decision Gate B 通过 → 先 P1a (4M smoke 验 tblite pipeline + Δ-ML 架构) → 再投 P1b (88M 正式验证)
- T1.2 V2 stage 2 (t1s2_autograd_88M_1ep_eqc0) step 26 OOM, 排查挖出两个叠加 bug。 (1) 显存: fp32+autograd+max_atoms=2700+DistanceBiasMLP 显存 4-6× 翻倍 (per-layer [B,H,M²] 偏置 + create_graph 二阶图), 80 GB 撑不住, step 26 是 sampler std=42 撞到接近 max 的 batch 概率累积。 (2) 静默更严重: prefix_unit `load_checkpoint_from_path` 不认 0524 单 writer save 的 `{"unit_state": ..., "config": ...}` 格式, key 检查 fall back 到 "treat blob as state_dict" 错路径, 整个 backbone 没 load, stage 2 从 fp32 随机 init 跑了 26 step, log 只有 WARNING 不 raise
- 两个 fix。 load: `prefix_unit.py:load_checkpoint_from_path` 加 `"unit_state" in checkpoint` 分支, drill 到 `blob['unit_state']['model']` (5-key 结构 dict 的 model 子 dict, flat 拓扑) 再走原来 DDP-prefix munging 逻辑。 OOM: 不是降 max_atoms 救火, 而是把 backbone default 整体改成 paper-aligned (shared_rbf ~4× 显存友好, max_atoms=2700 实测安全), 顺带把 chain 跟 paper-strict run 对齐。 5 项 default 改动一次到位 (`backbone/molecule_transformer.yaml` + `omol.yaml`): bias distance_bias → shared_rbf (含 shared_rbf_num_channels=64, shared_rbf_max_distance=10.0 都写进 default), use_chem_embed true → false, use_distance_bias true → false, equiv_coef 5 → 0 (T1.1 实证), element_refs fit → dense (对齐 reference run fc_transip_sharedrbf_L2scratch_eqc0)。 配合 CLAUDE.md 那条 strict launch 命令更新: 5 个 override 缩成 1 个 (只剩 `equiv_coef=5`), 其余全 default 自动生效, `+backbone.use_shared_rbf_bias=true` 再用 `+` 会 Hydra ERROR
- 端到端 smoke 验证 chain restart 路径。 stage 1 smoke (1 GPU bf16 direct max_steps=2): 新 default backbone (shared_rbf 14.4M params) instantiate ok, step 0 loss=43.28 finite, single-writer state.pt 落盘 252 MB 完整。 stage 2 smoke (load 上面 stage 1 step_0 ckpt, fp32 autograd shared_rbf max_atoms=2700, 单 80GB A100): load 三连日志干净 (Loading → Unexpected 6 个 `output_heads.omol_forces.force_mlp.*` 预期 stage 1 direct_forces=true vs stage 2 autograd 的 head 差异 → Successfully loaded), step 0 loss=33.05 比 stage 1 scratch 43.28 低 24% 是 load 实际生效的语义证据, fp32 autograd shared_rbf 二阶图 forward+backward 跑通无 OOM。 全 chain (path B 重跑 stage 1 → 2 → 3, ~30 hr ETA) 挂到算力池上跑
