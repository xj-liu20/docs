# 2026-05-26

> Project: FieldQuanta/predict_molecule · Tags: #infra #decision #training-launch

- T1.2 V2 chain stage 1 (t1s1_direct_88M_3ep_eqc0) 跑得慢, 排查 throughput 链路。 wandb sps 实测 2300 (32 rank aggregated, per-rank ~72 sps, per-step wall ~800 ms), profile trace per-step 184.7 ms — gap 615 ms 是 profile 没抓到的 non-compute overhead。 一开始凭 profile total kernel time 推 "shared_rbf 比 distance_bias 慢 2×, bf16+direct 没收益" 错论, 被 push back 后二轮按 4 个 ProfilerStep / profile window 正确 normalize, 修正成 CURRENT (shared_rbf bf16 direct) 比 REF (shared_rbf fp32+TF32 autograd) 快 1.8× (185 vs 338 ms/step), shared_rbf 跟 distance_bias compute 几乎相等 (61 vs 58 ms)
- dev 机单 GPU pure compute bench (bench_compute2.py, fake batch + tight loop forward+backward, 跳过 fit/dataloader): CURRENT 31.2 ms / REF 78.7 ms, **bf16+direct 2.53× faster on pure compute** 接近 3× 理论, eSEN 范式确认成立。 chain 慢不是 backbone/精度/force 模式选择问题
- 三层 breakdown 算清: pure compute 31/79 ms + DDP NCCL 153/259 ms + dataloader/IO 615/207 ms = total 800/545 ms。 **88M dataloader I/O 615 ms/step 是 CURRENT 的真 killer**, 是 REF 4M (207 ms) 的 3×。 DDP NCCL 加 150-260 ms/step 几乎吃掉 bf16 收益但 ring allreduce 固有不易优化
- cluster node 实测验证 IO 瓶颈: ssh 进 master node 看 free -g 显示 used 902 GB / 1003 GB total / buff/cache 只 93 GB / available 90 GB; vmstat bi=0 (本地 disk 没读); 网络 RX 5 秒 sample 138 MB/s sustained 持续在传。 一开始判 "FUSE bypass cache" 跟实际 4M dataloader 比 88M 快 3× 矛盾 (若完全 bypass 两者该一样慢), 二轮重判 root cause: page cache 在工作但 budget 被 902 GB 训练进程占用挤掉, 剩 93 GB cache; 4M 46 GB 装得下 93 GB → 100% hit; 88M 458 GB ≫ 93 GB → 20% hit 80% miss 走 AFS 网络 (138 MB/s = 80% miss 的 fetch 流量)。 真 root cause 不是 quarkfs 设计, 是 RAM 被进程吃掉的 effective cache budget 限制
- NVMe prefetch ROI 重估上调: 之前 ROADMAP 估 1.6-2.5× wall-time 收益保守了, 实测 138 MB/s 网络读 + FUSE daemon 路径开销支撑 **3-4× 实际收益** (88M chain 35 hr → 9-12 hr)。 待解 unknown: cluster node 本地 NVMe 容量是否 ≥ 458 GB / SenseCore allocation 是否 wipe 本地 disk / 902 GB used 中那 150-250 GB 不明部分是否真 leak。 IO 优化方案先 mark 着, 找资源中
- 几个 immediate apply: omol.yaml evaluate_every_n_steps 2500 → 5000 (减半 eval 阻塞频率), checkpoint_every_n_steps 10000 → 20000 (减半 ckpt 阻塞频率, 代价 crash 回滚多一倍 step), 当前 chain 不动 (34.5% 进度), 下次 chain 自动用新 default。 这两个改动加 ROADMAP T1 lesson learned 段 + Tier 2 item 10 (AFS → NVMe prefetch) 具体化 (两档实施方案 + 三层 breakdown 数据 + 实测 138 MB/s evidence)
- eSEN warmup 强度评估指标加入 ROADMAP T1 决策门 B' (2026-05-25 新增): 不看 chain 终态 Val-Comp 而看 stage 2 step 0 loss vs 假想 random-init 的差距, ≥30% 低 → T1.3 V4 (denoise + eSEN-direct + autograd 三段 pretrain) / 10-30% → V2 保持当前 chain / <10% → V3/V5 用 denoise 替代 stage 1。 等当前 chain stage 1 跑完到 stage 2 第一条 train log 出来直接判
- 排查 d032809 persistent_workers patch 跟 cache budget 的关系。 dev box 跑 10 cycle × 50 batch worker SPAWN-die loop (mimic d032809 修过的 leak 场景): sys used 48 → 69 GB (4 cycle 内涨完 +21 GB 后 cycle 5-10 plateau 不再涨), RSS 反而降, /dev/shm 仅 +20 文件可忽略。 跟原 commit 描述 "100-500 MB / val event 单调涨到 OOM" 不一致, 实际是 ceiling 21 GB 的自限 page cache warmup, kernel 自适应回收, 没 unbounded growth, 也不会 OOM (跟用户实际观察 "leak 不 OOM 就卡住" 吻合)
- 进一步挖 SPAWN context 是 22 GB / worker RSS 的真元凶。 SPAWN 每 worker 独立 Python interpreter + torch + fairchem + LMDB env, 没 COW; FORK 共享父进程页, COW 后单 worker 只 0.5-1 GB anonymous 独立。 32 worker × 22 GB SPAWN = 160-320 GB 纯 anonymous 浪费, 是 902 GB used 主因, 进而挤压 cache budget 到 93 GB。 UMA / fairchem upstream 都用 fork default (eSEN 一直没切 SPAWN)
- 反查 0521 commit 2799e72 切 SPAWN 的真实 trigger: 两次 CUDA init error 都在 DCP ckpt save 边界 (step 21907 / step 180800 right after step_180000 DCP save), 不是 generic fork+CUDA。 0524 commit 6529476 砍 DCP 后 trigger 消失, SPAWN 这层 trade-off 已过期。 dev box 两层 smoke 验证: 单 GPU fork + persistent + pin_memory + 25 ckpt × 500 step / 4 GPU DDP + NCCL + fork + persistent + pin_memory + 10 ckpt+barrier × 200 step, **0 crash**
- launcher 默认切 fork (commit `5746366`), 加 `PYTORCH_MP_SPAWN=1` env escape hatch 万一 cluster 撞老 crash 模式可一行回滚; cluster/sensecore.yaml 死字段 multiprocessing_context 同步更新 (注释说明 fairchem get_dataloader 不读 yaml, 实控在 launcher)。 omol.yaml eval/ckpt 频率减半同步 commit (`f4433c9`)。 当前 chain stage 1 (in-memory 加载 spawn) 不受影响, stage 2 重启 torchrun 自动用 fork; ckpt 格式跟 mp context 独立, stage 1 → 2 transfer 不动
- 预期 stage 2 启动后 free -g 看节点 used 从 902 → 250-300 GB, buff/cache 从 93 → 700+ GB, 88M 458 GB 完全 fit page cache, dataloader 615 ms → ~50 ms, chain wall 35 hr → ~12-15 hr (2.5-3× speedup), 同时验证 dev smoke 4-rank DDP+NCCL 是否能 generalize 到 cluster 32-rank
