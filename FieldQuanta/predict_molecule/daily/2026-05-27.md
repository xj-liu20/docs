# 2026-05-27

> Project: FieldQuanta/predict_molecule · Tags: #eval-result #decision #training-launch #infra

- T1.2 V2 chain 三阶段全跑完 (35.3 + 10.9 + 2.3 = 48.4 hr), 终态 vs T1.1 单 stage (4M × 80 ep) 反而更差: F avg 52.5 (vs T1.1 ~36, +46%), E avg 7.9 (vs T1.1 ~4.5, +76%); 比 paper 80ep 也更远 (F 1.23×, E 1.94×)。 V2 三阶段 chain 路线宣告失败
- Root cause: stage 1 (direct bf16) → stage 2 (autograd fp32) 切换 backbone 表征 mismatch, E 大幅 regression (metal E 7.0 → 11.5, +64%; neutralorg +46%, elytes +56%) — autograd 切后 force_coef:energy_coef = 15:5, force dominates 学习 energy 被牺牲; stage 2 lr 1e-4 + 1 ep 不够 backbone adapt; stage 3 anneal lr 5e-5 + 5 ep 太弱修不动
- T1V3 设计: 砍 V2 stage 1, 整 chain fp32+autograd 全程跟 T1.1 ref config 同 path 但数据 22×。 Stage 1: 88M × 2 ep autograd from scratch, lr=5e-4 paper baseline (~22 hr); Stage 2: 4M × 10 ep anneal, lr=1e-4 → 1e-6 cosine (~5 hr); 总 ~27 hr (V2 48 hr → -21 hr)。 commit `5abb10c`, 两个 yaml + run_t1v3_chain.sh 落地, 复用 V2 TCPStore 同步 helpers
- 新建 ~/xTB GPU 项目 (uv 管理, tblite + ase + numpy 装好, torch cu124 装中)。 README 中文写了 xTB 数学原理 (5 项能量分解 + H^0..H^3 各阶 + 最小基组 + SCF 6 步伪代码 + 解析力 CPSCF) + GPU 化路线 (4 步, PyTorch 重写 SCF → batched 推理 → 性能优化 → ML 流水线集成)。 跟现有项目对比: ByteQC/gpu4pyscf 是 DFT, PySEQM 是 NDDO 不是 xTB, **GPU + xTB 是公开空白区**
- xTB labeling bench (scripts/bench_xtb_omol_5k.py) 多轮迭代踩坑: 第一版 22 min scan classify (get_atoms 全集), 第二版用 metadata.npz 1 sec 分类; mult convention 写反 (OMol spin 字段 是 multiplicity 2S+1 不是 unpaired electrons 2S), 833/cat 跑 5 min 才发现全 fail; 修对 mult 后 1/cat smoke 仍 6/6 fail (SCF 不收敛 / eigenvalue solver fail / TS 结构 inherent 不稳), electrolytes 单 mol 47s。 OMol25 上 xTB labeling 远比想象难, ROADMAP 估 25 CPU-day 严重低估
- 找开源 xTB 标签数据集: **QMugs (665k drug-like 分子 / 2M conformer, GFN2-xTB + DFT 双精度)**, Thaler IDLe 论文 11M semi-empirical 数据集 (paper 2412.06064)。 这些可能直接用作 T3.M Stage P0 训练数据, 不必从 0 算 OMol25 的 xTB label
