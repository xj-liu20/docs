# 2026-05-28

> Project: FieldQuanta/predict_molecule · Tags: #design #training-launch #infra

- 主线切晶体材料 / Matbench-Discovery (F1 ≥ 0.9 north star, eSEN-30M-OAM 0.925 SoTA), 分子 fairchem_unit archive 不再主动推进 (T1.1/T1.2 已留 ckpt). 开 crystal 分支 (从 fairchem_unit 5abb10c)
- Tier 7 全栈 19 commits 一天推完: PBC min-image Cartesian distance + frac coord Fourier + LatticeEmbedder cell token + strain ε autograd (E/F/σ 共享 backward, 省 ~50% compute) + SO(3) aug 四块 implementation + 方向 bias 跟 multi-cutoff RBF 两条消融 prior (default off) + eSEN 2-stage chain (40ep direct bf16 → 60ep autograd fp32, ckpt warm-start strict=False) + per-task loss split log + UMA-aligned coefs/normalizer + DirectStressHead
- 数据 normalizer 自验证: scripts/fit_mptrj_refs.py 在 lmdb_evpa3 全 1,421,789 train fit, rmsd ours 0.7919 vs fairchem ESCAIP baseline 0.8001 delta -1.03% — 数值可信; 注意 fairchem MPtrj refs 是 ESCAIP baseline 不是 UMA flagship 训的 (UMA 主 train 是 omat/omol/omc/odac, 无 MPtrj), 用 fairchem ESCAIP 数值 copy 是验证过的
- 15:38 启 stage 1 SenseCore 2×8 A100 (`crystal_mptrj_s1_direct_40ep_20260528_153840`), bf16, 16 ranks 跑中, throughput 数没汇报到我能看的地方, ETA 等明天有 elapsed + step 才能算; chain script 自动接 stage 2 60ep autograd fp32
- 启动 cold cursor scan 慢 (3-4 min, 16 ranks 同时 hammer AFS metadata), 加 fast-index 路径用 lmdb b"length" 跳过 scan, val 实测 30s → 0.17s (~175×); 顺手修 torchtnt per-step warning spam (我 monkey-patch train_step 漏 type annotation, 加 `data: "AtomicData"` 字符串 forward-ref 解决). 都是 stage 2 启动时才生效, 当前 stage 1 不动
