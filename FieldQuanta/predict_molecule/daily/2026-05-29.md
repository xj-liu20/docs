# 2026-05-29

> Project: FieldQuanta/predict_molecule · Tags: #debug #training-launch #design

- 昨晚 stage 1 32 卡跑完 (87 min, 跟估的 88 min 几乎重合), F cos_sim 8 个 eval point 单调爬 -0.0001 → 0.0042, magnitude_err 32%。 用户判断 "不是没学, 是学得非常慢" 是对的, direct head 极限就这样
- 决定砍 stage 1 chain, 直接 single-stage `crystal_mptrj_autograd_100ep.yaml` from scratch, 100 epoch fp32 autograd, 一次到位避免 stage 1 wrong-sign stress 残留干扰
- 一连串 bug 修了 4 个:
  - `torch.linalg.inv/.det` 在 bf16 autocast 下不支持, cell `.float()` 临时转回 (commit `5db314c`)
  - stage 2 yaml 漏 override bf16, autocast 误用到 autograd 路径 (commit `53d7df3`)
  - autograd_100ep max_atoms=2700 fp32+autograd OOM (rank 30, 4 GiB needed / 3.9 free, 同卡有别人 75 GiB 进程), 砍 2000 (commit `32bf9e8`)
  - eval 时 backbone.forward 在 no_grad 上下文跑, 没建 autograd graph, head 后续 `autograd.grad(E, [pos, eps])` 报 "Tensors not used in graph"。 给 backbone forward 加 `@conditional_grad(torch.enable_grad())` (commit `2e78f7a`)
- 数据集换 `/mnt/afs/share/dataset/periodicSystem/MPtrj/aselmdb` (1,420,395 train, 158,367 val): 用户发现旧 `lmdb_evpa3` 路径 stress 符号是 pymatgen convention 错的, 新 aselmdb 是 ASE/fairchem 标准 (commit `56adca1`)
- E/F/σ coef 全调 1:10:100 (eSEN paper recipe, 之前我抄 UMA escaip 是 10:10:100 错的, 用户翻 paper 纠正)。 1:10:100 让 F 相对 E 强 10×, autograd path 下 F 学得更猛 (commit `80a6a3f`)
- eval autograd fix 用 mini smoke 验证 (16 records 1 GPU 重复 ~1500 mini-epoch 把 dataset 训成 overfit): cos_sim **第一次 eval 0.06, overfit 700+ mini-epoch 才破 0.20, 末态 max 0.36**。 pipeline 通过 + 无 crash + cos_sim 有上升趋势。 注意这是 16 records 记忆性 overfit 数, **不能外推真训练 cos_sim 速率** (真训 1.42M records 100 ep 的 generalize 行为机制不同)。 真训第一次 eval cos_sim 数等 step 2500 (~3.5 ep on full data) 出来才有 evidence
- F 学习速率分析: autograd 共享 backward, gradient 通过 -∂E/∂pos 流回 backbone, 比 direct head MLP 凭空学方向矢量物理上 sensible 得多 — 但具体 100ep 末态 cos_sim 没有 evidence, 估 0.4-0.8 之间 (stage 1 direct 0.004 跟 eSEN-30M-OAM 推算 0.85 之间), 等真跑完才知道
- 总 Tier 7 commit 数 24+, 全栈 ready 跑 100ep 真训
- 100ep run 到 epoch 22, E_pa 0.27 降到 0.16 在降, F_cos 0.16 爬到 0.28 慢爬, stress 0.0046 升到 0.0056 反而微升。 force 不是没学是学得慢, stress 二阶 autograd 量难学是 byproduct drift, 不是 bug
- 设计阶段1消融清单写进 ROADMAP, 编号即 ROI 即执行顺序, G0 锚点, G1 F方向 (dir/SH/multi-cutoff), G2 元素对, G3 stress coef, G4 lr, G5 norm, G6 动态cutoff, G7 C1平滑, G8 scale。 统一 30ep 短训单变量主看 F cos_sim, 两批脚本 run_ablation_group1.sh (G0+G1 共5个) + group2plus.sh (G2/3/4/5 共9个)
- 实现几个新架构开关, 都 default off, zero-init bit-identical, smoke 过: 球谐方向 bias (SH L=2/3, 比裸方向矢量强), RMSNorm + sandwich norm (norm_kind 一个 flag 连 QK-norm 一起换), 元素对 bias (同距离不同元素对作用不同, 做成乘性 gate 让元素对逐 channel 缩放 RBF, 是真的 距离乘元素对 耦合不是加性)
- 调研非等变 / transformer MLIP 架构 (PET-MAD / Orb / TransIP / eSEN / MD-ET / DiffCSP / Latent Ewald), 方向对, PET-MAD 就是非等变加周期晶体做到 SoTA。 PET 是 edge-centric 加局部 attention 加后处理精确符号化, 我们是 node 加全局 dense attention 加 SO(3) 增广。 Fourier distance 否决, frac 距离不等于实空间距离会伤力精度。 denoising 预训练 Orb/eSEN 都用记进阶段2
- 几个概念理清, QK-norm 防的是训练中权重范数漂移致 softmax 饱和, 除 sqrt(d) 只补静态维度尺度补不了动态漂移; 动态 cutoff 是改喂给 RBF 的距离 d/s_ij 不是改 RBF; envelope 修 kink 要 d_c 小于等于 L_min/2, 跟动态 cutoff 合体时 envelope 得用未缩放的 d_min 否则归零点漂移
