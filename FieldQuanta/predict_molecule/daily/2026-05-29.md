# 2026-05-29

> Project: FieldQuanta/predict_molecule · Tags: #debug #training-launch #design

- 昨晚 stage 1 32 卡跑完 (87 min, 跟估的 88 min 几乎重合), F cos_sim 8 个 eval point 单调爬 -0.0001 → 0.0042, magnitude_err 32%。 用户判断 "不是没学, 是学得非常慢" 是对的, direct head 极限就这样
- 决定砍 stage 1 chain, 直接 single-stage `crystal_mptrj_autograd_100ep.yaml` from scratch, 100 epoch fp32 autograd, 一次到位避免 stage 1 wrong-sign stress 残留干扰
- 一连串 bug 修了 4 个:
  - `torch.linalg.inv/.det` 在 bf16 autocast 下不支持, cell `.float()` 临时转回 (commit `5db314c`)
  - stage 2 yaml 漏 override bf16, autocast 误用到 autograd 路径 (commit `53d7df3`)
  - autograd_100ep max_atoms=2700 fp32+autograd OOM (rank 30, 4 GiB needed / 3.9 free, 同卡有别人 75 GiB 进程), 砍 2000 (commit `32bf9e8`)
  - eval 时 backbone.forward 在 no_grad 上下文跑, 没建 autograd graph, head 后续 `autograd.grad(E, [pos, eps])` 报 "Tensors not used in graph"。 给 backbone forward 加 `@conditional_grad(torch.enable_grad())` (commit `2e78f7a`)
- 数据集换 `/mnt/afs/share/dataset/periodicSystem/MPtrj/aselmdb` (1,420,395 train, 158,367 val): 用户发现旧 `lmdb_evpa3` 路径 stress 符号是 pymatgen convention 错的, 新 aselmdb 是 ASE/fairchem 标准 (commit `56adca1`)
- E/F/σ coef 全调 1:10:100 (eSEN paper recipe, 之前我抄 UMA escaip 是 10:10:100 错的, 用户翻 paper 纠正)。 1:10:100 让 F 相对 E 强 10×, autograd path 下 F 学得更猛 (commit `80a6a3f`)
- eval autograd fix smoke 实测: 第一次 eval cos_sim **0.21 (1:10:100 是 0.24)**, 比 stage 1 direct 40 epoch 末态 0.0042 强 50×。 autograd 强物理约束立竿见影, 之前 user 怀疑 autograd path bug 现在排除
- F 学习速率分析: stage 2 直接力 + autograd 共享 backward, gradient 通过 -∂E/∂pos 流回 backbone, 比 direct head MLP 凭空学方向矢量物理上 sensible 得多。 100ep 真训完才能跟 eSEN-30M-OAM (Matbench F1 0.925) baseline 对比
- 总 Tier 7 commit 数 24+, 全栈 ready 跑 100ep 真训
