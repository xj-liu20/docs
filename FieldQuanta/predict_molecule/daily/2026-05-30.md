# 2026-05-30 (加班)

> Project: FieldQuanta/predict_molecule · Tags: #eval-result #bug-fix #infra #decision

- 带 stress 符号 fix 的 100ep run 跑到 epoch 42, 符号修对了确实有效: stress drift 慢了约 4 倍 (旧 22ep 0.0046 升到 0.0056, 新 42ep 0.0045 升到 0.0050), E/F 也明显更好 (F cos 0.34, E_pa 0.115)。 但 stress 还在慢慢单调升, 没收敛
- 深挖 stress 不收敛, 结论是 MAE 这个指标在偏斜 target 上骗人, 不是 bug 也不是 loss form。 MPtrj stress 极偏, median 绝对值 0.0002 均值 0.00426, MAE 最优的常数预测就是猜零等于 0.00426。 我们 0.005 贴着零预测基本没学到, eSEN 0.0016 远低于均值才是真学会。 排查时单位 (aselmdb eV/Å³ 对) / 方差 / normalizer (autograd 下三者共享 energy rms 自洽) / coef (1:10:100 就是论文) / loss form (eSEN 三个 loss 类跟我们字节级一样) 全查实没问题
- 剩下是结构性原因, single-stage autograd from scratch 学 stress 纯二阶信号难, eSEN 那个 1.8 起点是 40ep direct head 热启动出来的, 加上非等变学 rank-2 张量本来就吃亏。 stress 后面单独立项, 最强杠杆是重做 direct 到 autograd 两段式
- 方向 bias 消融实测退化, abl_dir_L1 F cos 0.2968 跟 base 0.2972 一样提升为零。 根因 dir / SH bias 投影的是 d_hat 旋转等变矢量, 增广下同一物理 pair 所有朝向各出现一遍但 target 唯一, MLP 只能学到朝向无关的部分方向被洗掉。 单个 pair 平移加旋转下唯一不变量只有距离, 方向天生三体
- 实现 frac_edge bias (G1b), 用 min-image 分数位移 Δf 做方向 bias。 frac 坐标旋转下不变 (晶格跟着转) 平移规范下也不变, 所以能进 bias 且不被增广洗掉, smoke 实测特征旋转不变 3e-14
- min-image 距离在 d=L_min/2 有 C1 cusp (镜像突变致力冲激), 实现两个治本法 (G7): softmin 对 27 镜像 LSE 软最小, 远处等于硬 min 边界把 cusp 磨圆; image-sum 对镜像加 envelope 求和。 smoke 有限差分导数钉死, round 在 L/2 从 +1 突跳到 -1, softmin 平滑过零
- 晚上消融全挂, 查了半天先怀疑 infra / 盘满 (df 看 36% 空闲排除) / pod 抢占, 最后翻 rank20 的 log 才发现真因是 CUDA OOM。 一个 rank 显存炸死, 其他 rank 在 NCCL collective 上等它无错冻住, 看着像 infra hang。 更坑的是 heads.py 的 except 把 OOM 包成 "backbone detached input" 的误导消息, 把我带去查 disk / 抢占查了很久
- OOM 触发是批次里有个 392 原子的大结构, dense attention 把整批 pad 到 M=392, 注意力 [B,M,M] 张量按 M² 涨, 再叠上新加的 frac_edge / SH / imgsum 就爆了 80G
- 显存优化加了 gradient checkpointing, 8 层注意力激活 backward 时重算不存, 省最大头, fp64 smoke 验证 energy / force / 二阶 param-grad 全 bit-identical 无损 (不是近似)。 frac_edge_hidden 128 砍到 64。 campaign 统一开 checkpoint, max_atoms 从 1500 回到 2000, 单 run 慢约 30% 换不再 OOM 中断
- 修了两个脚本/代码坑: heads.py 不再把 OOM 伪装成 grad error 误导排查; 消融脚本之前 `|| continue` 加 exit 0 把失败全吞了, SenseCore 一直显示成功, 改成失败计数加 exit 1 加 FAIL_FAST 选项, 失败终于能暴露不再骗人
- 消融脚本按 group 拆成单独薄脚本 + 一个共享 lib, 各挂一个 job 可并行, 不再像旧 monolith 串行十几个一个挂全连坐。 补跑脚本 g1_rest 跑没完成的 sh_L2 / sh_L3 / mcrbf / frac_edge。 单 run 实测 ~3h @ 32卡 (base30 2.87h dir_L1 3.23h, 之前估的 1h 是错的)
