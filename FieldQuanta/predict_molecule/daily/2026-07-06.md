# 2026-07-06

> Project: FieldQuanta/predict_molecule · Tags: #experiment #result #decision

- lmax4 测完了,是个 κ-F1 权衡、净中性。edge384+bias8 加 lmax4(角度 SH 从 lmax3 的 16 通道加到 25):κ 0.557 比 lmax3 的 0.577 好、虚频从 3 降到 1,但 F1 掉到 0.837(lmax3 是 0.846),完整 CPS 0.756 跟 lmax3 的 0.757 打平。跟 edge 宽度那种 κ 和 F1 一起涨不一样,lmax 反而小小重引入了权衡。所以 lmax3 是甜点,别推 lmax5。
- finetune 时长测了,是 U 形、2ep 是甜点。edge384+bias8 的 κ:pretrain 0ep 是 0.664、2ep 是 0.577、4ep 是 0.624。0 到 2ep finetune 帮 κ(之前那个 finetune-κ 翻转的发现对),但过了 2ep 到 4ep κ 又退回去了,力-曲率过拟合还是回来。容量够让甜点从 edge64 的 0ep 移到 2ep,但不是越长越好。所以 2ep 就是甜点,不用加长也不用试 8ep。4ep 是崩前的 step10000、约等于 4ep,信号清楚。
- 把最优配置设成了默认,以后 SIZE=S/M/L 直接 bash 就是 lean_edge384 那套、不用再传一堆 override。改了 5 处:backbone 的 bias 从 64 改 8、edge_dim 从 64 改 384、edge_split_linear 开;run 脚本让 edge_dim 按 size 等于 dim,这样所有尺寸都是 edge:节点 1:1;顶层 config 的 use_geom_bilinear 关掉走 noPS,之前它在顶层被 override 成 on、盖过了 backbone 里的 false。验证过有效默认就是 edge384+bias8+split+noPS。一个提醒是 bias8 是默认,但 bias 甜点随 edge 变宽往窄移,M 的 edge576、L 的 edge768 甜点可能是 bias4,想榨再挂对照。
- 调参这几条轴 bias、lmax、finetune 时长基本都探明了:bias 甜点随 edge 移(256 要 16、384 要 8)、lmax3 甜点、finetune 2ep 甜点。当前最优是 edge384+bias8+lmax3+2ep、完整三项 CPS 0.757、离榜首 0.888 缺口 0.13。剩下真正没探的是容量轴:scale up 到 M/L、edge512、以及 MP 为主的大架构改动。M 的启动命令已经就绪(默认改完,SIZE=M 直接挂)。
