# 2026-07-07

> Project: FieldQuanta/predict_molecule · Tags: #experiment #result #decision

- 速度方案(fx_compile)昨天诊断完、今天 commit 存档(7c63009),昨天日报把结论补全了。就一句:两个后端在真 L=8 模型上都不给加速——inductor 被 dynamo 每个 batch 重编译堵死、flat-replay 摊平大图后 Python 逐算子跑比 eager 的 C++ autograd 还慢。停在这、flag 默认关,回主线 scaling up。
- 更正一个一直存在的参数量少报。训练日志那行 `AtomTransformerBackbone params` 只数了继承的 transip 基座 transformer(attention+ffn+embedding),没算 MoleculeTransformerBackbone 外挂的 edge channel(edge_dim=dim + outer-product + SH lmax3,是大头)和 output heads,系统性少报约 40%。实测全模型(逐档实例化、跟 M 的 checkpoint tensor 数对齐验证过):S(dim384)约 24.5M、M(dim576)54.9M、L(dim768)约 97M、XL(dim960)约 151M。之前口头说的 M 32.2M / S 14.4M 都是那行少报的数,真实 M 是 54.9M、M/S 2.25 倍。
- M 的 scaling up:pretrain 6ep 跑完(130,236 步,21:44 收尾,跟按 checkpoint 落盘间隔估的 ETA 分毫不差);finetune 2ep 已经挂上(等 pretrain 出 final ckpt 后 21:46 开跑)。挂了个等待器,finetune 完自动测三项 CPS(F1/κ/RMSD),跟 S 现在的 0.757 比,看容量轴放大到 M 是不是还在同时抬 κ 和 F1;是的话再考虑上 L。
