# 2026-07-15

> Project: FieldQuanta/predict_molecule · Tags: #experiment #decision

- S µP 的三档 finetune lr(1e-3/2e-4/1e-4)matbench 全跑完了,复用同一个 pretrain final、只重跑 finetune,κ 全量 103 + F1 every20。画出一条很干净的权衡曲线:finetune lr 越高,F1 越好但 κ 越差,单调的。lr 从 1e-4 到 2e-4 到 1e-3,F1_uniq 是 0.821→0.830→0.848 一路涨,κ_SRME 同向变差 0.617→0.613→0.655。
- 这直接推翻了前两天基于 val 力选的"1e-3 winner"。val 力 mae 最好不等于声子 κ 好 —— 高 lr 把 F1 顶上去,代价是把势能面的二阶曲率(声子吃这个)搞糙了。算 CPS 综合分,1e-3 和 2e-4 其实打平(0.693 vs 0.692),1e-3 只是把分数挪到了 F1、没挪到 κ,不是真的更好。
- 更要正视的是,µP 三档不管哪个 finetune lr,CPS 都比无 µP 的老基线(0.702)低一点,差主要在 κ(µP 是 0.61-0.66,老基线 0.568)。S 的 µP factor 是 1.0(隐藏层跟非 µP 字节等价),所以差异只可能来自 embedding 移桶或 finetune lr。假说是 embedding 移桶惹的 —— 它改成用 AdamW 5e-4 训、比留在 Muon 桶更猛,可能扰动了曲率。待一个 embedding 不移桶的对照来隔离。
- 不过这不否决 µP。µP 的价值本来就不在 S(factor=1.0 等价),在 M/L/XL 的免调 lr 和稳定性 —— 之前 S 短对照已经证了 µP-lr 把 M 的 skip 从上千次归零。S 上 CPS 略降是诊断信息、可接受代价,只要 M 上 µP 能让原本崩掉的训练稳定跑通并出好 CPS。下一步该挂 M 的完整训练,那才是 µP 真正要证明价值的地方,不在 S 上继续纠结。
- 顺带回顾了下 PET 那篇(arxiv 2305.19302)跟我们图处理的异同:都是非等变 transformer + autograd 力 + per-atom 能量求和这套顶层哲学。但图的组织相反 —— PET 是逐原子局部环境(cutoff 内邻居当 edge-token)+ 环境间消息传递,我们是全原子一条序列 + 全局 dense attention + 软距离偏置,edge/几何描述符只是旁路特征不是 attention 单位。我们 κ 偏弱、F1 偏强,大概就跟这个"全局 vs 局部"的选择有关,κ 主攻方向写的往局部 MP 靠也正是往 PET 这边挪。
- L/XL 结构先想的是纯加深(dim 固定 576、只加层,L20=102.7M、L44=198.3M),但深挖了一轮"原子模型该多深"的文献后改主意了。先纠正我自己一个说错的:之前口头说"原子模型都浅 2-8 层"是错的 —— 经典 ACE/MP 确实浅(MACE 2 层、Allegro 1-3),但前沿基础模型很深,EquiformerV2-153M 是 20 blocks、ORB-v2 是 15 层。关键是 ORB-v3 刚反转:把 15 层砍到 5 层、宽度 512 加到 1024,而 ORB-v3 正是我们对标的 κ 强模型(0.888)。再加一篇 AFM 剪枝(arxiv 2509.21694)实证深层消息传递冗余(层间 CKA 趋近 1、feature-shift 递减),MatterSim 剪到单层还能打平 Allegro。
- 有个关键映射让天平更偏向加宽:over-smoothing / over-squashing / under-reaching 都是消息传递的病,而我们是全局 dense attention,长程一层就够、根本没 over-squashing,只中 over-smoothing(注意力 rank collapse)。所以 MP 模型"堆层为了够远"那半理由对我们不成立,深度对我们卖点更弱;κ 是局部二阶曲率、剪枝证明一两层就拿到,深度不是 κ 杠杆。顺带把 LLM 偏深、原子偏宽的根因想清楚了:理论上深度分离(Telgarsky/Eldan-Shamir)说深度对组合函数指数级更省,Levine 的 depth-to-width 说自注意力深化只该 log 于加宽、GPT-3 其实太深;宽度更多是记忆容量(FFN key-value、约 2 bits/参数)。差异根因是目标函数的组合深度 —— 语言是深层组合(嵌套语法、多跳推理)所以吃深度,局部原子能量浅而宽(近视性、并行聚合、body-order 一层拿高阶)加上 κ 要光滑,所以吃宽度、罚深度。用户提的 DeepSeek mHC(流形约束超连接,把残差混合约束到双随机矩阵、Sinkhorn 投影)确实是治深层 rank collapse 的,对我们这种全局注意力比对 MP 模型还对症。
- 综合下来 L/XL 从纯加深改成"宽领先的均衡加宽加深",写进 run_omat24_oam.sh。head_dim 固定 96(头数=dim/96),edge 保持跟 dim 1:1,深度 8→8→9→11。
- 定结构时又踩了个参数量的坑、顺手实测修了。本来用 dim 平方拟合估 L 111.7M / XL 196.6M,后来实例化整个 backbone 一数发现错了:edge channel 有一批 per-layer 的小模块(msg_proj/gate 那些)是按层数长的,但挂在 encoder_stack 外面,我之前把它们算进了"固定非层开销",所以低估了。真数是 dim960/12 层 = 224M(不是 196.6M,差的 28M 就是漏掉的 edge 每层量)。既然真数超了 200M 目标,就各削一层贴目标:L 定 dim768/9 层 = 108M(约 2×M),XL 定 dim960/11 层 = 206M(约 200M)。
- 顺手实测了 XL 的显存来定 max_atoms(拿真 L 的 canonical_config 载全部 119 个 backbone 参数、只改尺寸到 XL,forward 加力的二阶反向,真实密度)。峰值大概是 2.2 + 52.8×(max_atoms/1000)GB(纯力),加上 stress 路约 +18% 和优化器/EMA 约 +2GB,真实约 4 + 62×(max_atoms/1000)。max_atoms=1000 时约 66GB、留 14GB 余量安全,1500 光算力就 81.6GB 撞顶。所以 XL max_atoms 定 1000/800(L11 理论能到 1100,保守留给大结构桶——实测一个过密的 300 原子合成桶在 1000 就 OOM 了,虽然那个偏悲观)。µP 的 lr 按 dim 自动缩不用手调,但深度增量 µP 不管,早期真出尖峰就加个 GPT-2 的 1/√(2L) 残差 init。
- 顺手估了下当前 M(mup+moonshot pretrain)还要多久:step 75000(12:35 写的、还在跑),吞吐 0.915 步/秒(两个连续 5000 步窗口 0.912/0.921 加全程均值都一致、没掉速),总步数按同配置的 nmlpsh M 6ep 约 130,236。剩 55000 步、约 16.8 小时,pretrain 明早 5 点多跑完;后面 finetune 另算。
- 顺手把参数量全量重测了一遍、修正文档。之前文档里几处数字都不对:coord-check 脚本打印的是裸 backbone 构造值(91.5M、跨 11 倍参数量那些),漏了 heads/edge 等组件,不等于真实训练参数量;我自己前面还一度把"XL"说成 dim960=202M 也是错的。这次直接对生产 ckpt 的 model state 求 sum(numel) 定案:S(dim384)24.45M、M(dim576)54.93M、L(加宽 dim768)97.57M,逐 tensor 数跟 M 的 194 个对齐验证过。关键澄清:真正训过的那个"200M"其实是 dim1248、edge 饿到 64(1:19)的 XL,实数 201.98M,不是 dim960;dim960 那档从没真训过、没 ckpt,文档里它的参数量都只是实例化估值。ROADMAP、周报、相关日报的参数量都改过来了。
- 改了 skip 判据。起因是个好问题:µP 对齐的是每层更新量,不是 grad_norm —— grad_norm 在优化器上游、µP 的 √width 缩放碰不到它。实测 grad_norm 随宽度涨得很快,S dim384 中位 61、M dim576 中位 122(差 2 倍,而宽度只差 1.5 倍,约 width^1.7)。所以按 grad_norm 固定绝对阈值判 skip(1000 是按 S 标定的)对 M/L/XL 越来越不合理,M 就多了 67 次 skip。改成相对判据:skip 看 grad_norm 是不是超过自己近期中位数的 8 倍,这样跨尺寸自适应、不用每档重标。但纯相对判据早期会失效(最开始滑动中位数没填满,而且 warmup 波动本就有 3-7 倍、是正常的),所以做成分阶段:早期窗口没满只用一个宽松的绝对上限 2000 兜底,窗口满了再切到相对判据、绝对 2000 继续作最后防线。DDP 下各卡梯度已同步、grad_norm 一致,所以各卡窗口和判定天然一致。默认开、可退回旧固定阈值。正在跑的 M 不受影响,L/XL 自动受益。
