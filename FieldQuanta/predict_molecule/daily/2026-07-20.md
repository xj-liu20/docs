# 2026-07-20

> Project: FieldQuanta/predict_molecule · Tags: #experiment #decision

- keller M 复现跑完了(pretrain 6ep + finetune 2ep)。finetune 曲线跟原版 nmlpsh_M 逐 bin 贴合,F loss 在 3k/6k/8k/10k 四个 bin 都落在 ±5% 内,稳定性也一模一样(skip 都 20、rollback 都 0、grad_norm p50 85 上下)。pretrain 曲线丢了 —— 07-18 磁盘满时 wandb 断在 step3600,训练靠 checkpoint 续命跑完剩下 126k step,中间曲线找不回。复现干净基本坐实,最终看 kappa。顺手挂了 watch_eval 自动测评,两个 target:移桶关补上 07-18 崩掉欠的 F1,keller final 一出接着测,convert→kappa 103 全量→F1 every20 一条龙。
- 移桶关 kappa 第二次跑出来 κ_SRME 0.6844 (100/103),跟 07-18 那次一模一样。两次独立跑同数,这个值坐实了 —— 之前 partial-pool 踩过坑,一次的数不算数。
- 今天最要紧的发现是翻 watch_eval 历史 summary 挖到的:keller 原版 M_edge576 不光 κ=0.4654,F1_uniq 还有 0.8582。之前只知道它 κ 好、不知道 F1。这条比 κ 本身更关键 —— keller 的 F1 几乎不输 moonshot+µP 移桶开(0.8582 vs 0.8630,只低 0.005),但 κ 大赚 0.26。折成 CPS(F1 占 50% + κ 占 40%,RMSD 两边都没测所以可比):keller 0.736 vs 移桶开 0.686,领先 0.050。之前担心"回 keller 要拿 F1 换 κ"根本不成立,是净赚。
- 把 grad_norm 这事在理论上捋清楚了,之前我自己讲混过。要分三个量:原始 grad_norm(clip_grad_norm 算的,在 optimizer.step 之前)、Muon update 幅度、dh(激活净变化)。Muon 的 Newton-Schulz 把梯度奇异值全拍成 1、magnitude 直接丢掉,所以 update 幅度只由 update_scale×lr 决定,跟原始 grad_norm 无关。原始 grad_norm 随宽度涨是纯维度效应(参数多、梯度张量大),在 Muon 里基本无害,不构成缩 lr 的理由。当年"keller L 用 lr 0.005 grad_norm 从 220 爬到 1230"那个是发散的症状不是原因,而且是相对 skip 判据(07-15)上线之前的观测,现在不一定还成立。
- 也搞清楚 keller 的 1/√d 是哪来的。Newton-Schulz 输出 U@Vᵀ 奇异值全 1,Frobenius 范数=√min(A,B),除以元素数开根得 RMS=1/√max(A,B);keller 的缩放因子是 √max(1,rows/cols),对 hidden 方阵恒等于 1,所以它其实什么都没做、直接继承了这个 1/√d。moonshot 则主动乘 rms_target·√max 把 RMS 拍成 0.2 常数。keller 的 1/√d 恰好抵消 fan_in 的 √d,这就是它 dh 天然 flat 的数学根源,也是为什么再给它套 width-scale 会 double-count 过缩。
- 给了 keller L 的启动命令,优化器完全照 keller M(lr 0.005/0.001、update_scale=keller、width-scale 和移桶都关),先看放大稳不稳。L 是 dim768/8头/9层/max_atoms1500,比 M 多一层、head_dim 保持 96。
- 晚上 F1 撞到 pool 拖尾:shard0/1 早完了,shard2 卡在 2890/4283、5.5s/struct 还要两小时,GPU 1 和 5 空转。拿这两块空闲卡提前跑 keller 的 kappa,用独立 RUN_NAME 避免跟 watch_eval 后续冲突,这样 keller 最关键那个数今晚就能看到,不用等到明天凌晨。watch_eval 后面还会自己再测一遍 keller,正好两次交叉验证。
- 夜里结果到齐了。keller 复现 κ_SRME = 0.4302 (101/103),我提前跑的那次和 watch_eval 自己跑的那次数值完全一样,两次独立执行交叉验证通过。比 keller 原版 0.4654 还好 0.035,刷新了全表最佳 κ,大概率是 07-15 上的相对 skip 判据让训练更稳。移桶关的 F1 也补齐了:F1_full 0.8375 / F1_uniq 0.8557。
- 四方盘面完整了(M, dim576):keller 复现 κ0.4302 / keller 原版 κ0.4654+F1 0.8582 / 移桶关 κ0.6844+F1 0.8557 / 移桶开 κ0.7274+F1 0.8630。折 CPS(F1 50%+κ 40%):keller 0.736~0.743,moonshot+µP 0.686~0.691,**keller 领先约 0.05**,而 F1 只差 0.005。回 keller 实锤,不存在拿 F1 换 κ。移桶那条线也彻底定性:移桶关 vs 移桶开 CPS 只差 0.005(κ 赚 0.009、F1 亏 0.004),跟前天归因精修说的"移桶是小配角"完全对上。
- keller L 挂上了,但放大确实不稳。前 9k step 很干净(grad_norm 72-160、skip 0-3、rollback 0),step 10k 起 grad_norm 开始爬(281→377→580),17k-18k 急剧恶化,skip 从 22 跳到 105 再到 135、rollback 2、grad_norm 冲到 1534(全程 max 8649)。也就是 L 跑了 18k step 就已经比 keller M 全程 130k step 还不稳(M 是 skip 97/rollback 1/p50 106)。
- 这个不稳的形状有信息:不是一开始就炸,是训练中后期才劣化,所以排除掉"lr 绝对值一上来就过大"和 dh 漂移(coord-check 测的 keller dh 本来就 flat,前 9k step 稳也印证了)。而且发现 coord-check 有个 gap —— 我当时跑的是固定 8 层下的 dims 384/576/768,但真实 L 是 9 层,深度增加那一维根本没测到。keller 的 1/√d 只补偿宽度(fan-in),不补偿深度。下一步先降 lr 到 0.004 试(不套 width-scale,coord-check 已证那个力度会过缩),或者跑个 L-but-8层 的诊断把深度和宽度的账分开。
