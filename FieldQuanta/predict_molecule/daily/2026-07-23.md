# 2026-07-23

> Project: FieldQuanta/predict_molecule · Tags: #experiment #infra

- sandwich M 进 finetune,挂 watch_eval 测 kappa + F1。finetune 跑得极干净,全程 skip 0、rollback 0,收尾时 grad_norm 只有 11。
- kappa pool 103 个材料全跑完,但 metric 脚本崩了,报 TypeError: float - list。查下来数据是好的、是脚本 bug,而且触发条件很讽刺 —— 我们写的是扁平 6 分量 Voigt(单 300K 点),DFT 参考按温度存成 list;上游 calculate_kappa_avg 把扁平 (6,) 塌成标量,参考仍是 list,相减就炸。以前不炸是因为总有 1-2 个材料虚频失败,那个函数对失败材料返回 np.array([nan]),整列保持 object dtype,numpy 标量减 list 能广播;这次 103 个全部成功、零虚频,列变成纯 float64,pandas 走向量化快路径就崩了。**模型太好反而触发了脚本 bug**。
- 修法是给我们的 κ 补温度轴,(6,) reshape 成 (1,6),让 avg 变成 1 元素数组能跟参考 list 广播,契合上游自己那句"srd 可能是温度 list、取 x[0]"的处理。回归验证过:keller_repro 加不加这个 fix 都是 0.4302,确认无副作用。
- **sandwich M 的 κ_SRME = 0.4205 (103/103)**,刷新全表最佳。对比 keller 复现 0.4302(101/103)、keller 原版 0.4654(102/103)。而且不是靠少算难材料换来的 —— 覆盖率同时是历史最高,103 个材料零虚频,声子谱全部物理合理。
- 到这里 sandwich 在 M 上是三赢:稳定性(grad_norm 后期 104 vs pre-LN 145、skip 27 vs 96)、上游(F loss 0.680 vs 0.705)、κ(0.4205 vs 0.4302)。原本以为要拿上游换稳定,结果三头都赢。F1 还在跑,出了才能定完整 CPS。
- 代码提交了两个 commit:sandwich norm 特性(含 gain small-init)和 kappa metric 的 bug 修复,分开提便于回溯。ROADMAP 也补了这一段。
- 下一步是 sandwich L,验证光靠 sandwich、不靠 weight decay 能不能治住 L 的放大不稳。如果成立,wd 三档扫那条路就可以降优先级 —— 毕竟 wd 临界值随尺寸变、每档都要重扫,而 sandwich 是架构级、尺寸无关的。
