# 2026-W21 周报

> 日期范围: 2026-05-13 ~ 2026-05-19
> Project: FieldQuanta/predict_molecule · Tags: #weekly

## 主要工作

| 主要工作内容 | 进展 | 可能存在的风险 |
|---|---|---|
| WignerD 反塌缩规则化探索, 5 种正则方案 (anti-trivial / per-block RMSNorm / cov whitening / Barlow Twins / VICReg) | 全部失败, l_0 share ≈ 1.0 锁死, l ≥ 1 channel 塌成 ~16 个 effective scalar (设计的 256-d 全塌); 决定换路线: `pure_invariant` (接受 l_0=1.0 当 invariant 训) + `distill_esen` (蒸馏 eSEN); 路线 A 代码已实现 | WignerD 整条路确认走不通, 4 周投入沉没成本; `pure_invariant` 分支搁置中, 没继续推, 后续若 strict baseline 也不行可能回头看; 蒸馏 eSEN 还没启动 |
| 切 TransIP strict-align baseline 路线, 新建 `fairchem_unit` 分支, 代码对齐 paper (loss / element_refs / 关 chem embed / TransIP-S sizing d=384 L=8 H=6) | 启动 `fc_transip_strict_sharedrbf_paper_4M80ep` 80 epoch 训练; 5 epoch 中期 eval F=144.5 vs paper 5-ep 146.6 (持平), E=25.3 vs paper 22.3 (1.13× 差) | 路线本身风险低 (paper 已发表), 但有 strict align 越对越偏的概率, 见下面 force plateau 一行 |
| 单层 RBF distance bias paper 复现撞 OOM, 实现 shared-RBF 显存优化 | TransIP 字面 `use_rbf_bias=true` rank 22 fp32+create_graph OOM at 78.97/80 GB (M=2000); 实现 `use_shared_rbf_bias=true` (拓扑等价 single-layer RBF, RBF expand 跨 LDE + 8 attn layer 共享一次), 实测显存 50.36 → 13.15 GB (4× 减), max_atoms 2000 → 2700 安全 | TransIP paper text 没提这个优化, 怀疑他们也有类似改动但没写; 复现成本高的另一个证据点 |
| Launcher infra 系统性修复: TF32 + JOB_TIMESTAMP race + FORK→SPAWN + multi-rank stability | TF32 启用 (commit `21aa674`, ~15% wall-time 省, force precision parity cos_sim=1.0); JOB_TIMESTAMP race 3 层根因 (pod 时钟不同步 + per-pod export + AFS sync lag) + 4 招修复 (commit `4bb20b7`); FORK→SPAWN cluster yaml 切换 (commit `1fd760b`, 解决 CUDA initialization error); rank-0 gate `resume.yaml` 写防止 AFS 并发冲突 | SenseCore pod 时钟不同步是基础设施问题, 我们这边只能 workaround 不能根治; AFS 跨节点 sync lag 仍依赖 30 min poll 兜底, 极端情况可能仍 fail; 跑了一天的 strict run 就是因为修复落地晚一天没享受到, 撞 FORK+CUDA 崩 |
| Strict baseline 中期 eval — 发现 **force MAE plateau, energy 反而追上 paper** | epoch 5 → 42: F 144.5 → 55.8 meV/Å (vs paper 80-ep 31.9 越来越远, 1.75×), E 25.3 → 4.80 meV/atom (vs paper 3.99 几乎追上, 1.20×); 最近 10 eval F 斜率 -0.2 meV/eval 明显 plateau, 按当前外推 80 epoch F ~52 / E ~3.85 (E 可能反超 paper) | F 追不上 paper 是个大风险, 如果 paper 真用了 MSE 那我们外推就是终态, 1.7× gap 不可接受; 整个 strict-align 路线核心 KPI 是追上 paper, F 不达标整条路线被怀疑 |
| **TransIP 7 分支 force loss audit** — 验证 paper text "MSE" 是否真实 | grep `main / dsornd / arun_main / rbf / rbf_2 / rbf_2_dist_rbf_llnl / rbf_edge_index` 7 个分支的 yaml + lightning 源码, **全部一致用 per-atom L2 norm 一阶 (sqrt(Σ(F-F*)²) → mean over atoms per graph → mean over graphs), 没一个用 MSE**; 代码里还留着 `# e_loss = F.mse_loss(...)` 注释痕迹说明作者试过 MSE 后主动弃用; 加上 plateau 曲线 fingerprint, 三项硬证据指向 paper text MSE 是 typo, paper 31.9 实际是 L2NormLoss 训的 | 这是个高价值发现但**待验证** — 切 L2NormLoss 后 F 能不能真突破 plateau 推到 32 量级, 才能反推 paper 公式真伪; 如果 L2 也 plateau 在 50+, 说明根因不是 loss 公式, 而是别的 (architecture / loss coef / Adam state / ... ), 整个 baseline 复现复杂度上一个台阶 |
| Tier 1.2 V2 三阶段训练 yaml 预实现 (88M direct → 88M autograd → 4M anneal) | 新增 `dataset/omol_88M.yaml` + `t1_stage{1,2,3}_*.yaml` 4 个配置 (commit `872c88a`); 88M dataset 完整性验证 (69 shards / 458 GB / metadata 含 87,687,161 samples); direct→autograd ckpt 跨载验证 (`strict=False`, 0 missing / 6 unexpected force_mlp.* 自动丢, energy_mlp byte-identical 迁移); bf16 direct + L_eqc 二阶图 stability dev box smoke 通过 | 已就绪可启, 但 88M × 3 ep 实际跑过 ~8-10 hr 没真实测过 throughput 跟外推一致; bf16 + L_eqc 二阶图只跑了 10 步 smoke, 长训稳定性未验证 |
| Daily/weekly docs 流程搭建 — GitHub `xj-liu20/docs` repo | 飞书 CLI 折腾失败 (企业 App scope 管理员只放 4 个, 个人 App setup 成本太高), 改用 GitHub private repo; 结构 `FieldQuanta/predict_molecule/{daily,weekly,notes}/`, daily 纯 bullet 摘要, weekly 周二写; 0513-0519 daily 全写完, 2 个 notes 长篇 (strict baseline eval 曲线 + TransIP loss audit) | 飞书集成放弃, 跨同事可见性受影响 (只有我能看); 后续要给老板/同事看需手动 share GitHub repo |

## 下周 todo (W22, 2026-05-25 ~ 05-31)

- **关键路径: L2NormLoss resume 跑完 80 epoch** (~20 hr 后出结果, 2026-05-20 ~ 21)
  - 看 F 终态能否突破当前 MSE plateau 推到 paper 31.9 量级
  - **决策门**: 如果 F ≤ 35, 实锤 paper text MSE 是 typo, ROADMAP / CLAUDE.md 全面更新 paper-align 表述, 把 L2NormLoss 设为 default
  - 如果 F 仍 50+ plateau, 调查更深: (1) `force_coef` 调到 30/45 试试 (2) AtomRefNormalizer std 是否合理 (3) 直接 fork TransIP lightning 代码本地复现一遍当 ground truth
- **Tier 1.1 `equiv_coef=0` ablation** (50.5 hr, 含 TF32 加速) — 测长训 L_eqc 是否还 essential
- **Tier 1.2 V2 三阶段第 1 步 88M direct pretrain** (~8-10 hr) — 启 stage 1, 出 eval gate 数字
- 如果 L2NormLoss 突破 plateap → Tier 2 LLM-stack 改造可以开始: RMSNorm / z-loss / DropPath / LLaMA-3 anneal / `torch.compile` 合成 80ep run
- 给 TransIP 作者写一封邮件确认 paper text "L_F = MSE" 是否 typo (低优, 可选)
- 整理一篇 notes: ROADMAP / CLAUDE.md 同步 update — "paper 实测公式 L2NormLoss, paper text 文本错"; 死路表 / TransIP strict-align knobs 段都纠正

## 本周关键里程碑达成情况

| 里程碑 | 状态 |
|---|---|
| ROADMAP §Week 1 (T0 + T1.1 并行 4-5 天) | ⚠️ 部分 — T0 strict 跑到 epoch 42 撞 bug, T1.1 ablation 还没启 |
| 决策门 A: strict baseline vs paper | ⚠️ 待 80 ep 完成 (走 L2NormLoss path) |
| Tier 1.2 V2 代码就绪 | ✅ commit `872c88a` |
| Launcher infra 稳定性 | ✅ 5 个 commit 落地 (JOB_TIMESTAMP / TF32 / SPAWN / stability fixes) |
| Daily / weekly 流程 | ✅ GitHub repo + 7 篇 daily + 2 篇 notes + 这篇周报 |
