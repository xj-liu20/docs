# 2026-W22 周报

> 日期范围: 2026-05-20 ~ 2026-05-26
> Project: FieldQuanta/predict_molecule · Tags: #weekly

## 主要工作

| 主要工作内容 | 进展 | 可能存在的风险 |
|---|---|---|
| T0 from-scratch L2 run 80ep 跑完 (验证 paper text MSE 是 typo, L2NormLoss 才是真公式) | 5 ep 中期 8 metric (4 cat × E/F) 全部 BEAT paper 5ep 40%, 全程 paper L2-gradient regime, 跳过 25k step MSE 早期震荡; ep 70.16 撞 CUDA init error 崩 (FORK+DCP), ckpt step_180000 安全, lost 800 step 但已 plateau 不亏; 终态 4 cat F 全部领先旧 resume run 8-15% | DCP+FORK 偶发崩还在, 短期靠 SPAWN 切换绕过, 长期是基础 infra 问题; 80ep 终态 F gap 仍 1.15-1.47× paper, 完全追平要等 T1.1 / T1.2 改进 |
| T1.1 `equiv_coef=0` ablation 80ep 终态验证 | 同 ep apples-to-apples 比 T0 (eqc=5): F 全 4 cat LOW 1.9-3.2 meV/Å, E 持平或略好; 终态 vs paper 80ep: **metal F 1.02× 基本追平**, bio E 1.04× 基本追平, 其他 F 1.08-1.16× / E 1.12-1.30×。 决策门: equiv_pair=true rotation augmentation 才是学等变性的真机制, L_eqc loss 项冗余且抢梯度, 后续 run 全按 equiv_coef=0; default 写进 omol.yaml | view2 forward 不能砍 (是 augmentation 的实际来源), wall-time 收益只 5% 不是想的 2× |
| DCP ckpt silent loss bug 排查 + fix (跨 13 个 run) | EVERY 长 run 缺 2-18 个 shard, 同一 run 同样 rank 持续缺。 Root cause: PyTorch DCP per-rank write 在 quarkfs AFS 上 open/write/fsync 返回 success 但 cross-pod sync 静默 drop 文件; all_reduce 看不到 exception, fairchem log "Saved" 成功, 永久数据丢失。 Fix: prefix_unit.save_state 改 master 单 writer torch.save 单文件 state.pt, DDP 所有 rank state 完全相同, 不需要 dedup sharding (commit `6529476`); 端到端 smoke 验证 byte-identical | 所有 0524 之前 ckpt 数据丢失不可恢复, 历史 run 终态 ckpt 全废, stage 1 必须重跑; 14M-100M 范围 OK, FSDP scale 必须改回 DCP |
| Tier 1.2 V2 chain restart (path B, 5 项 default paper-align 同步切换) | 第一次 chain stage 2 step 26 OOM + 静默 load_ckpt 错误 (load_checkpoint_from_path 不认 unit_state 格式, stage 2 26 step 全从 random init 跑了). 修两个 bug: load 加 unit_state 分支 (commit `d5ef678`), OOM 不降 max_atoms 而是整体切 paper-aligned (shared_rbf bias + 关 chem/dist_bias + equiv_coef=0 + dense element_refs 5 项 default 一次到位, commit `e441919`); end-to-end smoke 验证 stage 1 → stage 2 ckpt load 三连日志干净 + step 0 loss 33.05 比 scratch 43.28 低 24% 是 load 实际生效语义证据; chain 重启挂集群跑 (path B 重跑 stage 1+2+3) | 当前 chain 比 4M baseline 多 5 hr (~42 hr vs 37 hr), 仅 5-10% MAE 改善预期, ROI 中等; 真划算要 fork mp default 落地把 wall 砍到 12-15 hr |
| Throughput 排查 + non-compute overhead lesson | wandb 实测 2300 sps 比 profile 推 8640 sps 差 3.8×, gap 在 dataloader IO / DDP straggler / eval/ckpt 间歇阻塞。 一轮分析"shared_rbf 比 distance_bias 慢 2× / bf16+direct 没收益"是错论, push back 后二轮按 ProfilerStep 正确 normalize: CURRENT (shared_rbf bf16 direct) 比 REF (shared_rbf fp32+TF32 autograd) 快 1.8× profile 内, dev 单 GPU pure compute bench bf16+direct 2.53× faster 接近理论 3× 验证 eSEN 范式; 三层 breakdown: 31 ms compute + 153 ms DDP NCCL + 615 ms dataloader IO = 800 ms total; **88M dataloader IO 是真瓶颈** | profile 跟实测 gap 70% 的 lesson 学到了, 后续遇到 throughput 问题不能只看 profile total kernel time |
| 88M IO 瓶颈 cluster-side ssh 验证 + root cause 重判 | free -g 显示 used 902 GB / 1003 GB, buff/cache 只 93 GB; vmstat bi=0 但 网络 RX 138 MB/s sustained 持续在传; 一开始判 "FUSE bypass cache" 跟 4M 比 88M 快 3× 矛盾 (若 bypass 两者该一样慢), 二轮重判 root cause: page cache 在工作但 budget 被 902 GB 训练进程吃掉, 剩 93 GB cache; 4M 46 GB 装得下 → 100% hit; 88M 458 GB ≫ 93 GB → 20% hit 80% miss 走 AFS 网络 | 真 root cause 不是 quarkfs 设计, 是 RAM 被进程吃掉的 effective cache budget 限制, fix 路径找到 |
| SPAWN → fork mp default 切换 (内存压力根本解, 解 IO 瓶颈) | dev box 10 cycle leak bench 验证原 d032809 描述 "100-500 MB / val event 单调涨" 跟实际 ceiling 21 GB 自限不一致; 进一步挖 SPAWN 每 worker 22 GB RSS 是 cache 挤压元凶 (无 COW, 每 worker 独立 Python + torch + LMDB env); 反查 0521 切 SPAWN 真触发是 fork+DCP (不是 fork+CUDA generic), 0524 砍 DCP 后 trigger 消失。 dev box 两层 smoke 验证: 单 GPU fork + persistent + pin_memory + 25 ckpt × 500 step / 4 GPU DDP+NCCL+fork + persistent + pin_memory + 10 ckpt+barrier × 200 step, **0 crash**; commit `5746366` 切回 fork default + `PYTORCH_MP_SPAWN=1` escape hatch | dev box 4-rank smoke 不一定 generalize cluster 32-rank, cluster 上仍可能撞别的 fork+CUDA trigger; 当前 chain stage 1 (in-memory spawn) 不受影响, stage 2 重启自动用 fork 真上 |
| Δ-learning xTB pretrain 路线设计写进 ROADMAP Tier 3.M | 整体思路: OMol 大规模结构 + 低精度 xTB label pretrain → 加 head_delta 在 DFT fine-tune, `E_pred = head_xtb(h) + head_delta(h)`, autograd 自动拆 force sum 推理 0 overhead; 关键 framing: 这条路战略价值是 数据路线验证 (88M xTB ≈ 88M DFT? 若成立数据天花板从 88M → 1B PubChem); arxiv 二轮调研 4 paper (Ramakrishnan 经典 Δ-ML / Sammak 残差 MFNN / Gardner 2026 / Thaler 2024 IDLe), 残差架构跟全 trainable + zero-init delta head + Adam 隐式差异化 LR 融合方案; xTB 工具选 tblite (GFN2-xTB, ASE Calculator), skip UFF/MMFF/DFTB+ | T3.M 待 Decision Gate B 通过才启动, 现阶段只规划; xTB labeling 实测 electrolytes 单 mol 数十秒 (vs 估 1 sec), 100M 量级 label 成本可能 30-50× 高估 |
| 工程基础设施 5 个 commit 落地 | (1) TCPStore-based JOB_TIMESTAMP rendezvous (commit `d2a37e1`) 替代 AFS sync_file, 解 quarkfs cross-pod cache stale 跨 pod ts 分裂问题; (2) Chain script TCPStore + 阶段完成后 glob 最新 run_dir 拿 state.pt (commit `d300a1e`); (3) persistent_workers=True monkey-patch (commit `d032809`) — 当时为修 val leak, 0526 实测发现是 ceiling 21 GB 自限不是 unbounded leak, 决策保留; (4) ckpt load `unit_state` 格式分支 (commit `d5ef678`); (5) `evaluate_every_n_epochs` kwarg 暴露 (commit `1addbe5` + `44231fa`, default null 关 epoch-end eval, smoke 不撞) | quarkfs 一系列 stale-read 问题归一: 凡 cross-pod write-then-read 全走 TCPStore 不走 AFS, 形成原则 |
| ROADMAP 多次重大重构 | 加 Tier 2.P 数值精度 ladder + Tier 3.M Δ-ML 数据精度 ladder 同构两节; 加 Tier 7 Crystal extension (跨 domain 分子 → 周期晶体, 非等变 + 旋转 cell 增强); 重写 Stage P0 5 variant 数据效率验证矩阵; 加 eSEN warmup 决策门 B' (stage 2 step 0 loss vs random init, ≥30% 低 → V4 三段, 10-30% → V2 保持, <10% → V3 denoise 替代 stage 1); Tier 1 lesson learned 段加 "profile vs 实测 gap 70%" 公开认错 + 3 个修订路径 | ROADMAP 越来越长, 维护成本上升; 部分修订是连续推翻自己 (e.g. Tier 3.M 残差架构 v1 → v2 → v3 三轮), 反映探索过程但读起来累 |

## 下周 todo (W23, 2026-06-01 ~ 06-07)

- **当前 chain stage 1 跑完拿 stage 2 step 0 loss** (~ 12 hr 后), 用决策门 B' 评估 eSEN warmup 强度, 决定 T1.3 设计方向 (V2 保持 / V3 denoise 替代 stage 1 / V4 三段 pretrain)
- **stage 2 启动后实测 fork mp default 在 cluster 32-rank 是否 generalize**: 关键看 free -g 节点 used 是否从 902 → 250 GB, buff/cache 93 → 700+ GB, 88M dataloader 615 → < 100 ms; 若 cache 真扩大 dataloader 真加速, chain wall 35 hr → 12-15 hr 实现 (2.5-3× speedup); 若没扩大或 cluster 上撞 fork+CUDA crash, 一行 `PYTORCH_MP_SPAWN=1` env 切回 spawn
- **xTB labeling pipeline 数据**: dev box bench 1/cat smoke 拿 per-cat 实测时间, 估算 4M / 88M labeling 真成本 (electrolytes 单 mol ~30+ sec 比预期高 30×, 大概率 88M 需要数百 CPU-day 而非 25 CPU-day, NVMe + 多节点并行才可行)
- **T1.2 V2 chain 终态 Val-Comp**: stage 2 + stage 3 跑完拿 4-cat E/F MAE, 跟 T0/T1.1 / paper 比 — 是否值得继续 T1.x 路线
- 决策门 B (T1.2 V2 vs T0): 若 T1.2 终态 ≥ 10% MAE 改善, 启 Tier 3.M Stage P0 4M xTB 数据效率验证; 若 < 10%, 重新评估 88M 路线 ROI
- 等 stage 2 fork mp 实测后, **commit 之前 ROADMAP T1 IO 优化预估**改成 fork 单一 fix 收益 (从 NVMe prefetch 估 3-4× 退到 fork 单切预期 2.5-3×, 后者更便宜更直接)

## 本周关键里程碑达成情况

| 里程碑 | 状态 |
|---|---|
| 决策门 A: paper text MSE = typo 假说终态实证 | ✓ T0 from-scratch L2 80ep 跑完, 全 cat 改善 |
| Tier 1.1 `equiv_coef=0` ablation 80ep 终态 | ✓ metal F 1.02× paper 基本追平, equiv_coef=0 成 default |
| DCP silent loss bug 排查 + fix | ✓ commit `6529476` 单 writer save 端到端验证 |
| Tier 1.2 V2 chain 启动跑通 | ⚠️ 部分 — stage 1 跑了 65% (12 hr 剩), stage 2 第一次撞 OOM 已修, 这次走完整 chain |
| 88M IO 瓶颈 root cause 定位 | ✓ FORK mp default 切换 (cache budget 93 → 700+ GB 预期), 等 cluster 实测 |
| eSEN warmup 决策指标 (决策门 B') | ✓ 写进 ROADMAP, 等 stage 1 完成 + stage 2 step 0 loss 实测拿数 |
| T3.M Δ-ML + Tier 2.P 精度 ladder + Tier 7 Crystal 设计 | ✓ 全部写进 ROADMAP |
| Daily / weekly 流程 | ✓ 5 篇 daily + 这篇 weekly 推到 GitHub |
