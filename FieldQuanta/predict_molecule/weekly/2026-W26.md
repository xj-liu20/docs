# 2026-W26 周报

> 日期范围: 2026-06-18 ~ 06-23
> Project: FieldQuanta/predict_molecule · Tags: #weekly

## 主要工作

| 主要工作内容 | 进展 | 可能存在的风险 |
|---|---|---|
| 确认非等变模型的最佳结果, 并把三个"小改架构"的实验全部验完 (加长地基训练 / 边通道径向重分配 / 对齐 eSEN 的损失配比) | 当前最佳 **unique F1 = 0.8224**(F1 = 把没见过的新材料结构, 用模型弛豫后判断它稳不稳的准确率, 越高越好), **已超过中间目标 `eSEN-30M-MPtrj` 的 0.819**。三个小改实验都证实"到顶": 地基训练 6 遍是峰值(第 8 遍反降到 0.8196)、边通道重分配是死路(0.8050 < 对照 0.8100)、对齐 eSEN 损失配比中性(两个配置都只 +0.001~0.002, 在噪声内)。 | "小改架构"这条路确认榨干, 0.8224 离北极星 **F1 ≥ 0.9** 还差 ~0.08, 再涨只能靠**放大模型**, 但放大到底能涨多少还未知(要等 scaling 曲线) |
| 转向放大模型到 **200M 参数(现在 20M, 放大 10×)**, 摸清能不能跑、慢在哪、还能不能更快 | **200M 能稳定训练**(实测配置: 宽度 d=1248、每批 3500 原子、学习率降到 0.003 后梯度稳)。但**慢 —— 头一个结果要 ~3.5-4 天**。根因: 算"守恒力"必须做二阶求导, 显存被占满 → 每批塞的数据量被逼小 → 训练步数翻倍。常规省显存的三条路(重算激活 / 直接预测力 / 半精度 bf16)**全部堵死**(原理冲突或精度崩, bf16 实测在近平衡点把力打成噪声); 唯一提速路 = **算子融合**, 已扒到业界 deepmd-kit 的现成实现模板 | 200M 的 F1 还没出(3.5-4 天后); 放大兑现度未知; 算子融合是独立工程(估提速 **1.3-1.5×**, 几天工作量, 等放大方向确认再投) |
| 训练基础设施抗崩加固 | 修复了 autoresume 自动续训的两个"雪崩坑": ① 已训完的阶段不再被误重跑; ② **单节点崩溃(显存爆 / 节点掉线)不再连累全体对不齐、空转 20 次浪费算力** —— 改成"确定性崩溃就喊停、瞬态崩溃才重试 + 多节点同步重启"。已提交版本库。 | 多节点同步重启(elastic)只在开发机验了逻辑, 真实多节点首次崩溃恢复要盯一眼 |

## 下周 todo (W27)
- 等 200M 第一个 scale F1, 看放大兑现多少, 量 scaling 斜率 → 定 scale 目标
- (若放大方向确认) 投算子融合 infra (deepmd-kit `make_fx` 模板), 提速 1.3-1.5×
