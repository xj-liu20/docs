# 2026-W29 周报

> 日期范围: 2026-07-08 ~ 2026-07-14
> Project: FieldQuanta/predict_molecule · Tags: #weekly

## 主要工作

| 主要工作内容 | 进展 | 可能存在的风险 |
|---|---|---|
| 排查 moonshot 放大到 M 训练不稳,写 coord-check 定位病根 | S 稳、M 同配置反复 skip/rollback 中断;coord-check(逐层探针)发现放大时激活和 attention logit 都平,只有每步更新量 dh 随宽度漂 ~√width,即学习率没随宽度调对 | 探针只测主干内部,最终 readout 输出层的缩放规则没单独验证,放大到 L/XL 若能量收敛异常需先查这里 |
| 实现 µP 宽度缩放修复(隐藏层 lr×√(base/width)),coord-check 复验 | Muon 隐藏层更新按宽度缩、embedding 移到 AdamW 常数 lr;dh 漂移从 ×1.37 压到 ×1.07,跨整个宽度范围(dim 288→960)近乎恒定,越大越有效 | 只补了 µP 缺的这一环(未做 init/attention 全套),当前够用,更大规模可能需补 |
| 验证 µP 真实训练有效,固化 moonshot+µP 为默认配置 | M 换 µP 学习率后 skip 从上千降到零、精度不降;µP factor 按宽度自动缩,S/M/L/XL 统一传 lr=1e-3 不必每档重扫,已写入默认 config | 复现 keller 老配置需显式指定旧参数,已在 config 注明 |
| 扫 finetune 学习率得到反直觉结论,顺带修 wandb lr 显示 bug | 以往认为 finetune 必须大幅降 lr,实测反而 1e-3(与 pretrain 同值)最稳、val 最好;µP 移桶改了 param group 顺序致 train/lr 记错,已修 | finetune 结论目前只在 S 验证,M/L 是否成立待后续确认 |

## 下周 todo (W30)

- 用固化的 moonshot+µP 默认从头训练 M 模型,评估稳定性与 CPS 三项指标(F1/κ/RMSD)
- 把 µP 宽度缩放继续验证到 L/XL,确认放大时 lr 无需重扫
- 探索以局部消息传递为主的架构改动,继续压低声子分 κ
